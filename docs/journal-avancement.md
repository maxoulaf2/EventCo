# Journal d'avancement

> Ajouter une ligne par tâche terminée, format : `- AAAA-MM-JJ — [Lot X] Description concise de la tâche (+ décision notable si applicable)`

- 2026-09-16 — [Lot 1] Correction bug : connexion via lien magique inopérante en navigation privée. **Décision notable** : abandon du cross-origin dev (`SameSite=None`) au profit d'un frontend/API same-origin via proxy Vite (`client/vite.config.ts`, `/api` + `/hubs`), cookie de session repassé en `SameSite=Lax` (`Secure` conditionné à `!IsDevelopment()`) dans `AuthController`.
- 2026-08-31 — [Lot 1] Initialisation de la solution .NET (Clean Architecture Domain/Application/Infrastructure/Api + projets de tests xUnit)
- 2026-08-31 — [Lot 1] Initialisation du projet React (Vite + TypeScript + Tailwind CSS v4)
- 2026-08-31 — [Lot 1] Docker Compose (API + PostgreSQL + pgAdmin optionnel)
- 2026-08-31 — [Lot 1] Modèle de données Domain : `User`, `MagicLinkToken`, `Event` (agrégat racine), `EventParticipant`, `EventTask`, règles métier encapsulées dans l'agrégat `Event`
- 2026-08-31 — [Lot 1] Remplacement des levées génériques de `DomainException` par 18 exceptions métier spécifiques, une par règle violée
- 2026-08-31 — [Lot 1] Abstraction de la résolution du temps : suppression des appels directs à `DateTime.UtcNow` dans le Domain au profit d'un `IDateTimeProvider` injecté
- 2026-08-31 — [Lot 1] Configuration EF Core (`DbContext`, mappings, première migration `InitialCreate`) appliquée sur PostgreSQL
- 2026-08-31 — [Lot 1] Authentification passwordless (backend) — génération et envoi du magic link, `POST /api/auth/request-link`. **Décision** : MediatR étant passé sous licence payante en production, remplacé par une abstraction maison (`ICommand`/`ICommandHandler`/`ICommandDispatcher`) à utiliser pour tous les futurs Commands/Queries jusqu'à décision définitive du développeur
- 2026-08-31 — [Lot 1] Remplacement des tests xUnit+Moq du Command `RequestMagicLink` par des tests Gherkin/Reqnroll (InMemory, sans mock de repository) — convention actée dans `conventions-code.md` §1.4
- 2026-09-01 — [Lot 1] Premier test d'intégration bout-en-bout `EventCo.Api.Tests` pour `POST /api/auth/request-link`, contre un vrai PostgreSQL (Testcontainers)
- 2026-09-01 — [Lot 1] Extension de l'approche Gherkin/Reqnroll à `EventCo.Api.Tests` — conteneur PostgreSQL et `WebApplicationFactory` désormais démarrés une seule fois pour tout le run
- 2026-09-01 — [Lot 1] Authentification passwordless (backend) — validation du token et création de session, `POST /api/auth/verify` : token consommé (`MagicLinkToken.Consume`), compte `User` créé automatiquement à la première connexion (réutilisé sinon), cookie de session httpOnly déposé. **Décisions notables** :
  - Abstraction `ICommand`/`ICommandHandler`/`ICommandDispatcher` étendue avec un pendant à réponse (`ICommand<TResponse>`/`ICommandHandler<TCommand,TResponse>`/`Send<TResponse>`), nécessaire dès ce premier Command qui doit renvoyer un résultat à l'API (utile aussi pour les futures Queries du lot 2). Les deux interfaces `ICommand`/`ICommand<TResponse>` sont volontairement indépendantes (pas d'héritage) pour éviter une ambiguïté de surcharge sur `Send` côté `CommandDispatcher`.
  - Session = jeton signé maison (payload + signature HMAC-SHA256, même famille que le hash des magic links) plutôt qu'une dépendance JWT (`System.IdentityModel.Tokens.Jwt`) — le cadrage laisse le choix ("JWT ou identifiant de session") et ça évite d'introduire une nouvelle lib externe pour ce besoin (cf. prudence licences/maintenance actée précédemment). Secret + durée de vie configurables via la section `Session` (`appsettings.json`, à surcharger en production).
  - Cookie posé en `SameSite=Lax`/`Secure`/`HttpOnly` ; **point à retrancher lors de la tâche frontend** : le frontend Vite (`:5173`) appelant l'API (`:5000`) en cross-origin en dev nécessitera CORS + `SameSite=None` (donc HTTPS même en dev) pour que le cookie soit accepté — non traité ici, hors périmètre backand seul.
  - `EventCo.Api.Tests` : le port `IEmailSender` (jusqu'ici non substitué, seul `LoggingEmailSender` réel était utilisé) est désormais remplacé par un double observable (`RecordingEmailSender`) via `ConfigureTestServices` dans `ApiWebApplicationFactory`, seul moyen de récupérer le token brut du lien de connexion pour enchaîner sur la vérification dans un test bout-en-bout (le hash seul est stocké en base, par conception).
- 2026-09-01 — [Lot 1] Authentification passwordless (frontend) — formulaire de saisie d'email (`RequestMagicLinkForm`) + page de confirmation (`CheckEmailPage`), ainsi que la page de traitement du lien reçu par email (`VerifyMagicLinkPage`, route `/auth/verify?token=...`, indispensable pour que le flow magic link soit utilisable de bout en bout — non listée séparément dans le backlog mais découle directement de `MagicLink:VerificationUrlBase`). **Décisions notables** :
  - Ajout de `react-router-dom` (routage `/`, `/auth/check-email`, `/auth/verify`) et `@tanstack/react-query` (mutations `useRequestMagicLink`/`useVerifyMagicLink`), conformément à `conventions-code.md` §2.2 — rien n'était encore installé.
  - Organisation `features/auth/{components,hooks,api.ts,types.ts}` + `shared/lib/api.ts` (wrapper `fetch` : base URL via `VITE_API_URL`, `credentials: 'include'`, extraction du message d'erreur depuis `ProblemDetails`/`ValidationProblemDetails`).
  - Ajout de `"strict": true` dans `tsconfig.app.json`/`tsconfig.node.json` : absent du scaffold initial alors que `conventions-code.md` §2.2 l'exige — corrigé au passage plutôt que d'introduire du nouveau code non strict.
  - **Tâche manquante ajoutée et traitée** (rule 5) : configuration CORS côté API (`Program.cs` + section `Frontend:BaseUrl` dans `appsettings.json`), sans laquelle les appels `fetch` du frontend (`:5173`) vers l'API (`:7166` en https dev) sont bloqués par le navigateur — point explicitement laissé en suspens dans le journal du 2026-09-01 (tâche `VerifyMagicLink` backend).
  - Vérification de bout en bout faite manuellement en dev (API + Vite + Postgres réels, sans navigateur — outil non disponible dans cet environnement) : preflight CORS, `POST /api/auth/request-link`, lien loggé par `LoggingEmailSender`, `POST /api/auth/verify` (cookie `eventco_session` déposé, `Set-Cookie` correct), et réutilisation du même token → 400 `MagicLinkTokenAlreadyConsumedException` bien renvoyé. Le message d'erreur brut du backend (contient `TokenId`/dates) n'est volontairement pas affiché tel quel à l'utilisateur : `VerifyMagicLinkPage` affiche un message générique en cas d'échec.
  - Les messages de validation FluentValidation (ex: email invalide) restent en anglais (pas de configuration de localisation côté backend) — hors périmètre de cette tâche, atténué par la validation HTML5 (`type="email"`, `required`) côté formulaire qui limite les cas où ce message apparaît.
- 2026-09-02 — [Lot 1] Infrastructure de tests frontend en Gherkin, trois couches distinctes (convention actée dans `conventions-code.md` §2.3) : comportement mocké (**Vitest** + Testing Library + **MSW**, Gherkin via `@amiceli/vitest-cucumber`), non-régression visuelle (**Playwright** `toHaveScreenshot` + **playwright-bdd**, projets `mobile`/`tablet`/`desktop`), E2E nominal (**Playwright** + **playwright-bdd** contre la vraie stack docker compose). Scénarios d'exemple écrits sur le flow magic link existant, les trois suites tournent et passent (vérifié en local, y compris l'E2E contre l'API + PostgreSQL réels démarrés le temps du test). **Décisions notables** :
  - Maintenance de `vitest-cucumber` et `playwright-bdd` vérifiée avant adoption (dernières releases récentes, projets actifs) conformément à la prudence actée après l'épisode MediatR.
  - Extraction de `AppRoutes` hors de `App` (`client/src/App.tsx`) pour permettre aux tests de rendre les routes avec un `MemoryRouter`/`QueryClient` de test sans dupliquer la déclaration des routes.
  - Piège rencontré : avec `vitest-cucumber`, chaque étape Gherkin (pas chaque scénario) est un test Vitest à part entière — un `afterEach` global aurait réinitialisé les handlers MSW entre deux étapes d'un même scénario. Reset déplacé dans `AfterEachScenario`, propre à chaque fichier `.steps.tsx`.
  - Piège rencontré : `loadFeature(path)` résout `path` relativement au fichier `.steps.tsx` appelant (pas au cwd), et le parser français retire les mots de liaison (`que`/`qu'`) du texte des étapes — les deux ont fait échouer les premiers essais avant d'être compris et documentés dans `conventions-code.md` §2.3 pour ne pas les re-découvrir.
  - Les captures de référence de la couche visuelle sont sorties de `.features-gen/` (généré, ignoré par git) vers `tests/visual/__screenshots__/` (versionné) via `snapshotPathTemplate`, sans quoi elles auraient été perdues à chaque régénération des specs.
- 2026-09-02 — [Lot 1] E2E nominal isolé de la base de dev, à la demande du développeur : `docker-compose.e2e.yml` dédié (services `postgres-e2e`/`api-e2e`, projet Compose `eventco-e2e`, ports `5433`/`5001`, volume jetable), démarré/arrêté automatiquement par `globalSetup`/`globalTeardown` de `tests/e2e/playwright.config.ts` — `npm run test:e2e` n'a plus besoin de `docker compose up -d postgres api` en préalable. **Décisions notables** :
  - Migration de la base `eventco_e2e` (neuve à chaque run, `down -v` en teardown) faite depuis `global-setup.ts` (`dotnet ef database update`, même commande que celle documentée dans CLAUDE.md pour la base de dev) — une première version ajoutait un flag `Startup:MigrateOnStartup` dans `Program.cs` pour migrer au démarrage de l'API, revenu en arrière à la demande du développeur : le code de prod ne doit pas porter de branche conditionnelle pour un besoin propre aux tests. `Program.cs` est donc resté inchangé.
  - `name: eventco-e2e` explicite en tête de `docker-compose.e2e.yml` : sans lui, Compose dérive le nom de projet du nom du dossier et les deux fichiers compose (situés côte à côte) partageraient par défaut le même projet, au risque d'interactions entre stacks.
  - Vérifié en conditions réelles : stack de dev (`eventco-postgres`/`eventco-api`) déjà démarrée en parallèle pendant le run e2e, restée intacte (conteneurs, ports, données) après le run et son teardown.
- 2026-09-02 — [Lot 1] Middleware d'authentification : lecture du cookie de session (`eventco_session`), résolution de l'utilisateur courant. **Décisions notables** :
  - Implémenté comme un vrai scheme d'authentification ASP.NET Core (`SessionAuthenticationHandler : AuthenticationHandler<AuthenticationSchemeOptions>`, `src/EventCo.Api/Auth/`) plutôt qu'un middleware ad hoc peuplant `HttpContext.Items` : ça permet d'utiliser `[Authorize]` sur les futurs endpoints protégés (lot 2) et les 401 automatiques, sans code à dupliquer. `ISessionTokenService` étendu avec `ValidateSessionToken` (vérification HMAC en temps constant via `CryptographicOperations.FixedTimeEquals`, puis expiration) — le type privé `SessionTokenPayload` de `SessionTokenService` est devenu le record public `SessionTokenData` (`EventCo.Application.Common.Interfaces`), réutilisé pour la création et la validation du token.
  - Ajout de `ICurrentUserService` (`EventCo.Application.Common.Interfaces`, implémenté côté Api via `HttpContextAccessor`/claims) : c'est le point d'accès à l'utilisateur courant dont les Commands du lot 2 auront besoin pour les règles d'autorisation créateur/co-organisateur/participant.
  - **Tâche manquante ajoutée et traitée** (rule 5) : endpoint `GET /api/auth/me` (`[Authorize]`, Query `GetCurrentUserQuery` suivant le pattern `ICommand<TResponse>`/`ICommandDispatcher` existant) — nécessaire à la fois pour vérifier le middleware de bout en bout (aucun endpoint protégé n'existait encore) et pour le besoin réel du frontend de restaurer une session existante au chargement de l'app (le cookie étant httpOnly, le frontend ne peut pas savoir seul si une session valide existe). `IUserRepository` complété avec `GetByIdAsync`.
  - Tests Gherkin/Reqnroll ajoutés dans `EventCo.Api.Tests` (`Auth/CurrentUser/`, même pattern que `Auth/Verify/`) : cookie de session valide (200 + email attendu), absence de cookie (401), cookie falsifié (401).
- 2026-09-02 — [Lot 1] Service d'envoi d'email configuré : `SmtpEmailSender` (`src/EventCo.Infrastructure/Emailing/`), un seul sender SMTP générique plutôt qu'un SDK par fournisseur — Mailtrap (dev), Resend et SendGrid exposent tous un relais SMTP standard, donc le même code fonctionne avec les trois selon la config (`Email:Smtp:*`, nouvelle section `EmailOptions`/`SmtpOptions`). **Décisions notables** :
  - Lib retenue : **MailKit** (MIT, activement maintenu par jstedfast) — vérifié avant adoption conformément à la prudence licences/maintenance actée après l'épisode MediatR.
  - Sélection à l'exécution entre `SmtpEmailSender` et l'existant `LoggingEmailSender` selon que `Email:Smtp:Host` est renseigné ou non (`DependencyInjection.AddInfrastructure`) : par défaut (`Host` vide dans `appsettings.json`), les emails restent seulement loggés — aucun risque d'envoi accidentel tant qu'un compte n'est pas explicitement configuré.
  - Les identifiants ne sont jamais commités : `UserSecretsId` initialisé sur `EventCo.Api` (`dotnet user-secrets init`), section `Email:Smtp` à renseigner en local via `dotnet user-secrets set` (commandes ajoutées dans `CLAUDE.md`) pour un compte Mailtrap, en production via variables d'environnement (Resend/SendGrid), même approche que `Session:Secret`.
  - Pas de tests dédiés à `SmtpEmailSender` : comme `LoggingEmailSender`, c'est une implémentation fine d'un port externe (`IEmailSender`), déjà substituée par `RecordingEmailSender` dans `Application.Tests`/`Api.Tests` — cohérent avec l'approche existante (`conventions-code.md` §1.4).
- 2026-09-03 — [Lot 1] CI (GitHub Actions), `.github/workflows/ci.yml`, sur push `main` : 4 jobs parallèles — backend (`dotnet test`, `Domain`/`Application`/`Api.Tests`, ce dernier via Testcontainers), frontend comportement (`npm test`), frontend non-régression visuelle, frontend E2E nominal. Pas encore de déploiement automatisé (pas d'hébergement à ce stade). **Décisions notables** :
  - `Api.Tests` (Testcontainers) et `test:e2e` (`docker-compose.e2e.yml`) tournent sans configuration Docker particulière : le daemon Docker est préinstallé sur les runners `ubuntu-latest`.
  - Job E2E : `dotnet-ef` installé en outil global dans le workflow (pas de manifest d'outils local dans le repo), version alignée sur `Microsoft.EntityFrameworkCore.Design` (`10.0.4`) — même commande `dotnet ef database update` que celle utilisée par `global-setup.ts` en local.
  - **Non-régression visuelle — changement de process** (à la demande du développeur) : le job `frontend-visual-tests` tourne dans l'image Docker officielle Playwright (`mcr.microsoft.com/playwright:v1.62.1-jammy`, tag aligné sur `@playwright/test`), et non plus sur le poste de dev (Windows) — le rendu des fonts diffère selon l'OS et aurait rendu les captures de référence incomparables entre local et CI. `npm run test:visual:update` ne doit donc plus être lancé en local ; un second workflow dédié (`update-visual-baselines.yml`, déclenchement manuel) régénère les captures dans le même environnement et les dépose en artefact téléchargeable — le développeur les compare et les committe lui-même (pas de commit automatique, la validation reste un geste humain explicite). En cas d'échec en CI, le rapport Playwright (captures attendue/obtenue/diff) est aussi remonté en artefact pour permettre cette comparaison depuis la pipeline. Documenté dans `conventions-code.md` §2.3.
  - Déclenchement limité à `push` sur `main` (portée demandée) : pas encore de vérification sur les Pull Requests avant fusion — à reconsidérer plus tard si utile, non traité ici faute de demande explicite.
  - **Corrections post mise en place** (constatées au premier run) :
    - Actions GitHub (`checkout`/`setup-dotnet`/`setup-node`/`upload-artifact`) bumpées en `v7`/`v6`/`v7`/`v7` : les versions initiales (`v4`) tournent encore sur Node 20, deprecated par GitHub au profit de Node 24.
    - Job E2E : `dotnet restore` manquant avant `dotnet ef database update` (appelé par `global-setup.ts`) — celui-ci build `EventCo.Api`/`EventCo.Infrastructure` et a donc besoin d'un `project.assets.json` déjà généré.
    - `tests/visual/playwright.config.ts` et `tests/e2e/playwright.config.ts` : `outputDir`/`reporter` html passés en chemins explicites (`path.join(import.meta.dirname, ...)`) — la résolution par défaut de Playwright ne pointait pas de façon fiable vers `tests/visual|e2e/playwright-report`, ce qui faisait échouer l'upload d'artefact de rapport en CI (`No files were found`) quand ces suites échouaient.
  - **Publication des rapports Playwright en échec sur GitHub Pages** (à la demande du développeur, pour consultation directe au navigateur sans télécharger/dézipper l'artefact) : nouveau job `publish-reports` dans `ci.yml`, déclenché uniquement si `frontend-visual-tests` et/ou `frontend-e2e-tests` échoue, republie le site Pages en entier à chaque run concerné (`/visual/`, `/e2e/`, `index.html` de sommaire) — un job qui n'échoue pas cette fois ne fournit pas de rapport, donc l'ancien rapport de ce job disparaît du site ; accepté explicitement (pas d'historique à conserver, un seul développeur qui consulte au fil de l'eau). **Point important signalé et accepté par le développeur** : sur un repo privé hors GitHub Enterprise Cloud, un site Pages n'est pas restreignable — il est accessible publiquement par son URL (non indexée/partagée) même si le repo reste privé.
    > Bloqué : nécessite une action manuelle unique du développeur — Settings → Pages → Build and deployment → Source = "GitHub Actions" (impossible à faire depuis le workflow lui-même). Le job échouera tant que ce n'est pas fait.
  - **`update-visual-baselines.yml` ouvre désormais une Pull Request** (à la demande du développeur, pour merger facilement les nouvelles captures) au lieu de ne déposer qu'un artefact à télécharger : découpé en 2 jobs — `regenerate-screenshots` (image Docker Playwright, inchangé, dépose toujours l'artefact `visual-baselines` en complément) puis `open-pull-request` (runner nu, `git`/`gh` déjà présents) qui synchronise `client/tests/visual/__screenshots__/` avec l'artefact, et si le diff n'est pas vide, pousse sur une branche fixe `chore/update-visual-baselines` (force-push, réécrite à chaque déclenchement) et ouvre une PR — ou réutilise celle déjà ouverte sur cette branche s'il y en a une, cohérent avec le choix déjà fait pour les rapports Pages (pas d'historique à conserver). `gh` n'étant pas présent dans l'image Playwright (seul `git` l'est), la partie PR est déportée sur `ubuntu-latest` plutôt que d'installer `gh` manuellement dans le conteneur.
- 2026-09-03 — [Lot 2] Création d'un événement (`POST /api/events`) : Command `CreateEventCommand`/`CreateEventCommandHandler` (pattern `ICommand<TResponse>` existant), créateur = utilisateur courant résolu via `ICurrentUserService` (endpoint `[Authorize]`), délègue à `Event.Create` (déjà présent et testé côté Domain depuis le lot 1) qui inscrit automatiquement le créateur comme participant `Organizer` ayant rejoint. Nouveau `IEventRepository`/`EventRepository` (`AddAsync` seulement pour l'instant — étendu au fil des tâches suivantes du lot, comme `IUserRepository` au lot 1). **Décisions notables** :
  - Pas de validation FluentValidation dupliquant la règle Domain (titre vide) au-delà de `NotEmpty` — cohérent avec l'approche déjà en place (ex: `VerifyMagicLinkCommandValidator`/`Token`), le Domain reste la source de vérité et le test associé existe déjà dans `EventCo.Domain.Tests`.
  - `EventsController.Create` renvoie `201` via `Created(uri, body)` avec une URI construite à la main (`api/events/{id}`) plutôt que `CreatedAtAction` : l'endpoint `GET /api/events/{id}` (tâche suivante de ce lot) n'existe pas encore pour que `CreatedAtAction` puisse le référencer.
  - **Piège rencontré et corrigé avant qu'il ne casse la CI** : le step Gherkin `Etant donné une session ouverte via l'API pour "..."`, nécessaire pour tester ce nouvel endpoint `[Authorize]`, existait déjà dans `CurrentUserSteps.cs` (lot 1) — l'ajouter une seconde fois dans les steps de `EventCo.Api.Tests/Events/Create` aurait rendu ce step ambigu pour Reqnroll (deux méthodes `[Binding]` différentes matchant le même texte). Extrait dans un binding partagé (`Support/AuthenticatedSessionSteps.cs`) qui pose le cookie obtenu dans une classe de contexte injectée par Reqnroll (`Support/SessionContext.cs`, partagée entre bindings d'un même scénario) — `CurrentUserSteps.cs` a été adapté pour consommer ce contexte au lieu de dupliquer la logique.
  - Vérifié en conditions réelles : `dotnet test` sur les trois projets de tests backend (`Domain.Tests`, `Application.Tests`, `Api.Tests` — ce dernier nécessitant Docker, démarré manuellement pour l'occasion, absent par défaut de cet environnement) tous verts, y compris les scénarios existants du lot 1 (non-régression de l'extraction du step partagé).
- 2026-09-04 — [Lot 2] Consultation d'un événement (`GET /api/events/{id}`) : Query `GetEventByIdQuery`/`GetEventByIdQueryHandler` (même pattern `ICommand<TResponse>`/`ICommandDispatcher` que `CreateEvent`), `IEventRepository` étendu avec `GetByIdAsync`. Réutilise le contrat `EventResponse` existant (même représentation qu'à la création). **Décisions notables** :
  - **Nouveau cas de mapping HTTP dans `GlobalExceptionHandler`** : jusqu'ici toute `DomainException` était mappée en 400, ce qui ne convient pas à une ressource introuvable par son identifiant (§3.2 de `conventions-code.md` prescrit 404). Nouvelle exception `EventNotFoundException` (`EventCo.Domain.Events.Exceptions`, même famille que `EventTaskNotFoundException`/`ParticipantNotFoundException`), interceptée par un cas spécifique du switch **avant** le cas générique `DomainException` (l'ordre du pattern matching est significatif ici) — mappée en 404 plutôt que 400. Réutilisable telle quelle par les tâches suivantes de ce lot (modification, suppression) qui auront le même besoin.
  - **Point d'architecture signalé pour validation** : `GET /api/events/{id}` est accessible à tout utilisateur authentifié (`[Authorize]`), sans vérifier que l'appelant est bien participant de l'événement — le cadrage (`cadrage-projet-eventco.md` §modèle de rôles) sous-entend qu'un événement n'est consultable que par ses participants, mais cette règle d'autorisation n'est pas encore implémentée nulle part dans le code (aucun pattern établi pour la faire respecter dans un handler de Query, aucun mapping 403 encore en place) et la tâche dédiée à ces règles (« Logique métier : distinction créateur / co-organisateur / participant ») est explicitement le prochain élément du backlog de ce lot. Restriction volontairement non anticipée ici pour ne pas improviser un pattern d'autorisation avant cette tâche dédiée — à traiter (et re-couvrir cet endpoint) à ce moment-là.
  - Pas d'`Include` sur `Participants`/`Tasks` dans `EventRepository.GetByIdAsync` : la réponse ne contient que les champs de l'événement lui-même (comme à la création), la liste des participants n'étant pas demandée par cette tâche (elle relève de « Frontend : page de détail d'un événement (infos + liste des participants) », plus bas dans ce même lot).
  - Vérifié en conditions réelles : `dotnet test` sur les trois projets de tests backend, tous verts (`Domain.Tests` inchangé à 27, `Application.Tests` 12 dont les 2 nouveaux scénarios Gherkin, `Api.Tests` 11 dont les 3 nouveaux — Docker disponible dans cet environnement pour cette tâche).
- 2026-09-04 — [Lot 2] Liste des événements de l'utilisateur courant (`GET /api/events`) + page d'accueil frontend correspondante (tableau de bord après connexion) : tâches manquantes identifiées et traitées en cours de route (rule 5, cf. notes ci-dessus dans le backlog), à la demande explicite du développeur suite à un constat qu'aucun endpoint ni page ne permettait de retrouver ses événements après création/invitation. **Décisions notables** :
  - `IEventRepository.GetByParticipantUserIdAsync` (`Events.Any(p => p.UserId == userId)` avec `Include(Participants)`) plutôt qu'une nouvelle méthode sur l'agrégat `Event` : la sélection porte sur plusieurs événements, pas sur le comportement d'un agrégat unique. `GetMyEventsQueryHandler` recombine ensuite, pour chaque événement retourné, le rôle et le statut (`HasJoined`) du participant courant via la collection publique `Event.Participants` — sans exposer de nouvelle méthode sur l'agrégat, cette lecture ne modifie aucun état.
  - Réponse (`EventSummaryResponse`) volontairement distincte de `EventResponse` (détail) : elle porte `Role`/`HasJoined`, propres à l'utilisateur courant et non à l'événement lui-même, absents du contrat de détail. `GET /api/events` (liste) et `POST /api/events` (création) cohabitent sur la même route `api/events`, distingués par le verbe HTTP — cohérent avec `GET /api/events/{id}` déjà en place.
  - Frontend : nouvelle route `/events` (`EventsDashboardPage`, feature `features/events/`) devenant la destination après connexion — `VerifyMagicLinkPage` y redirige désormais automatiquement (`useEffect` sur `isSuccess`, via `useAppNavigate.toEvents`) au lieu de rester sur l'écran de confirmation sans suite. Un événement où l'utilisateur est invité mais n'a pas encore rejoint (`HasJoined = false`) est affiché avec un badge « Invitation en attente » plutôt que d'être masqué, en prévision du flow d'invitation à venir plus bas dans ce lot.
  - Un 401 sur `GET /api/events` (session invalide/expirée) redirige silencieusement vers la page de connexion (`<Navigate>` dans `EventsDashboardPage`) plutôt que d'afficher une erreur — seul filet de sécurité minimal ajouté ici : il n'existe encore aucune garde de route générique (protected route) ni restauration de session au chargement (`GET /api/auth/me` existe depuis le lot 1 mais n'est consommé par aucune page) — signalé pour arbitrage, non traité ici faute de tâche dédiée dans le backlog.
  - Scénario Playwright (non-régression visuelle / E2E) initialement non ajouté pour cette page (cf. note ci-dessous, revenu dessus le même jour à la demande du développeur) : couche comportement (Vitest/MSW/Gherkin) ajoutée dès cette tâche en revanche (`features/events/EventsDashboard.feature`), cohérente avec `RequestMagicLink.feature`.
  - Vérifié en conditions réelles, au-delà des suites automatisées : parcours complet dans un vrai navigateur (Playwright piloté manuellement, faute de `chromium-cli` disponible ici) contre la stack `docker-compose.e2e.yml` déjà utilisée par `test:e2e` — demande de lien, extraction du token depuis les logs de `LoggingEmailSender`, vérification, redirection vers `/events` (liste vide), création d'un événement via l'API avec le cookie de session du navigateur, rafraîchissement (l'événement apparaît), puis accès à `/events` sans session (redirection vers la connexion) — aucune erreur console. `dotnet test` (3 projets) et `npm test`/`npm run build` (frontend) tous verts après ajout.
- 2026-09-04 — [Lot 2] Suites Playwright (E2E nominal + non-régression visuelle) pour le tableau de bord des événements, à la demande explicite du développeur — revient sur le report noté juste au-dessus. **Décisions notables** :
  - **E2E** (`tests/e2e/features/mes-evenements.feature`) : la page de création d'événement n'existe toujours pas, donc l'événement affiché est créé via un appel direct à l'API (`POST /api/events`, avec le cookie de session récupéré depuis le contexte du navigateur) plutôt que depuis l'UI — seule la partie "Given" (mise en situation) contourne l'UI, le parcours vérifié (connexion par lien magique, affichage du tableau de bord) reste bout-en-bout. Nouveau step partagé `je me connecte avec un lien magique`, récupération du token de connexion initialement via les logs Docker de `eventco-api-e2e` — **remplacé le même jour par Mailpit, cf. entrée suivante**.
  - **Non-régression visuelle** (`tests/visual/features/apparence-tableau-de-bord.feature`) : back mocké au niveau réseau (`page.route`), comme `apparence-pages-auth.steps.ts` — deux scénarios (liste avec une invitation en attente, liste vide), 3 tailles d'écran chacun.
  - **Captures de référence générées via Docker plutôt que reportées au développeur** : contrairement à la décision du 2026-09-03 (« `test:visual:update` ne doit plus être exécuté en local », rendu du texte différent selon l'OS), les captures ont été produites ici en lançant `npm run test:visual:update` **dans l'image Docker Playwright exacte de la CI** (`mcr.microsoft.com/playwright:v1.62.1-jammy`, alignée sur `@playwright/test`) plutôt que directement sur le poste Windows — la contrainte réelle (rendu Linux, pas Windows) est respectée sans attendre un déclenchement manuel du workflow `update-visual-baselines`. `node_modules` du conteneur isolé dans un volume Docker nommé (jamais écrit dans celui de l'hôte, aux binaires natifs Windows) pour ne pas casser l'environnement de dev après coup ; volume et image supprimés après usage. Les 6 captures pré-existantes (pages d'authentification) ressorties identiques (aucune écriture Git dessus), seules les 6 nouvelles (tableau de bord × 3 tailles × 2 scénarios) ajoutées.
  - **`tsconfig.app.json`/`tsconfig.playwright.json`** : correction connexe découverte en cours de route (le développeur avait commencé à déplacer les fichiers `.feature`/`.steps.tsx` colocalisés vers `client/tests/behavior/`, sans encore adapter les projets TypeScript) — `tsconfig.playwright.json` (pensé pour Playwright/Node, `moduleResolution: nodenext`, pas de lib DOM) ratissait aussi `tests/behavior/` et cassait `npm run build` sur ces fichiers Vitest. `tests/behavior` déplacé dans l'`include` de `tsconfig.app.json` (même projet que `src/test/setup.ts`, qui porte l'augmentation de types `@testing-library/jest-dom`) et exclu de `tsconfig.playwright.json`.
  - `MyEvent.status` (frontend, `features/events/types.ts`) typé en union `'Draft' | 'Planned' | 'Completed' | 'Cancelled'` (`EventStatus`) plutôt que `string`, aligné sur l'enum `EventStatus` du Domain.
  - Vérifié en conditions réelles : `npm run test:e2e` (3 scénarios verts, dont les 2 nouveaux) et génération des captures de référence (12 scénarios verts) tous deux exécutés contre la stack dédiée / l'image Docker CI ; `npm test`/`npm run build` (frontend) et `dotnet test` (backend, non modifié par cette tâche) revérifiés verts après coup.
- 2026-09-04 — [Lot 2] Remplacement de l'extraction du lien magique par les logs Docker (E2E) par un vrai faux serveur SMTP, à la demande explicite du développeur (piste évoquée par lui en question, cf. `conventions-code.md` §2.3 pour la décision détaillée). `mailpit-e2e` (image `axllent/mailpit`) ajouté à `docker-compose.e2e.yml`, `api-e2e` reconfiguré pour lui envoyer réellement ses emails (`Email__Smtp__Host`/`Port`, bascule vers `SmtpEmailSender` — jusqu'ici jamais exercé par une suite de tests) ; les steps interrogent son API HTTP (`GET /api/v1/search?query=to:<email>` puis `/api/v1/message/{id}`) plutôt que `docker logs`. **Décisions notables** :
  - `global-setup.ts` : fonction d'attente déjà présente pour l'API (`waitForApi`) généralisée en `waitForHttp` et réutilisée pour sonder Mailpit avant de lancer les tests, plutôt que dupliquer la même logique de polling.
  - Healthcheck Mailpit repris de celui déjà intégré à l'image (`mailpit readyz`) plutôt qu'improvisé (l'image ne contient qu'un binaire Go, pas de shell/`wget`/`curl` pour un `CMD-SHELL` classique) — `depends_on: condition: service_healthy` sur `api-e2e`, même mécanique que `postgres-e2e`.
- 2026-09-04 — [Lot 2] Modification d'un événement (`PUT /api/events/{id}`) : Command `UpdateEventCommand`/`UpdateEventCommandHandler` (même pattern `ICommand<TResponse>`/`ICommandDispatcher` que `CreateEvent`/`GetEventById`), délègue à `Event.UpdateDetails` (déjà présent et testé côté Domain depuis le lot 1, jusqu'ici jamais exercé par aucun Command). `IEventRepository` étendu avec `UpdateAsync` (l'entité étant déjà trackée par le `DbContext` depuis `GetByIdAsync`, `UpdateAsync` se contente d'un `SaveChangesAsync` explicite, symétrique à `AddAsync`). Réutilise `EventNotFoundException`/le contrat `EventResponse` déjà en place. **Décisions notables** :
  - **Autorisation non implémentée ici, choix délibérément aligné sur `GetEventById`** : comme signalé dans le journal du 2026-09-04 (tâche `GetEventById`), aucun mécanisme d'autorisation par rôle n'est encore câblé dans le code, et la tâche dédiée (« Logique métier : distinction créateur / co-organisateur / participant ») reste le prochain élément du backlog. Le cadrage (`cadrage-projet-eventco.md` §3.2) autorise le créateur **et** les co-organisateurs à modifier un événement — `PUT /api/events/{id}` est donc pour l'instant accessible à tout utilisateur authentifié (`[Authorize]` seul), sans vérifier son rôle vis-à-vis de l'événement, cohérent avec le choix déjà fait pour la consultation plutôt que d'improviser un pattern d'autorisation avant la tâche dédiée. **À re-couvrir cet endpoint à ce moment-là.**
  - **Piège évité avant qu'il ne casse les tests** : les scénarios `Application.Tests`/`Api.Tests` de cette tâche avaient besoin du même step Gherkin `Given` que `GetEventById` (« un événement ... prévu le ... au lieu ... ») / `GetById` (« un événement ... créé via l'API ») pour poser un événement existant — dupliquer ce texte dans une nouvelle classe `[Binding]` l'aurait rendu ambigu pour Reqnroll (même piège que le step de session du 2026-09-03, deux méthodes `[Binding]` différentes matchant le même texte). Côté `Application.Tests`, reformulé (« un événement **existant** ... ») pour éviter la collision sans toucher au test existant. Côté `Api.Tests`, le step a en revanche été extrait dans un binding partagé (`Support/ExistingEventSteps.cs`) avec un nouveau contexte partagé `Support/EventContext.cs` (même mécanique que `SessionContext` pour le cookie) — `GetByIdSteps.cs` adapté pour le consommer au lieu de dupliquer sa propre logique de création.
  - Vérifié en conditions réelles : `dotnet test` sur les trois projets de tests backend, tous verts (`Domain.Tests` inchangé à 27, `Application.Tests` 18 dont les 3 nouveaux scénarios Gherkin, `Api.Tests` 16 dont les 3 nouveaux — Docker disponible dans cet environnement pour cette tâche, non-régression confirmée sur `GetById` après son adaptation au contexte partagé).
  - **Correction post-relecture (développeur)** : le scénario « Modification avec des données valides » ne vérifiait que le titre et le lieu, pas la date — `EventDate` n'était donc pas couvert alors qu'elle fait partie des champs modifiés par `UpdateDetails`. Step `Alors l'événement modifié a pour date "..."` ajouté dans `UpdateEvent.feature`/`UpdateEventSteps.cs`.
- 2026-09-04 — [Lot 2] Suppression d'un événement, réservée au créateur (`DELETE /api/events/{id}`) : Command `DeleteEventCommand`/`DeleteEventCommandHandler` (pattern `ICommand`/`ICommandHandler<TCommand>` sans réponse, jusqu'ici jamais exercé par aucun Command du lot 2 — tous retournaient un résultat). `IEventRepository` étendu avec `DeleteAsync`. **Décisions notables** :
  - **Première vérification d'autorisation réellement câblée dans ce lot** (contrairement à `GetEventById`/`UpdateEvent`, dont le journal signalait explicitement l'absence de contrôle de rôle en attendant la tâche dédiée « Logique métier : distinction créateur / co-organisateur / participant », plus bas dans ce même lot). Possible ici sans attendre cette tâche car le besoin de suppression ne porte que sur une seule règle simple et déjà entièrement outillée côté Domain (« réservé au créateur », pas de nuance créateur/co-organisateur/participant à trancher) : nouvelle méthode publique `Event.EnsureCanBeDeletedBy(actingUserId)`, qui réutilise telle quelle la vérification privée `EnsureActingUserIsCreator` déjà existante (même famille que `PromoteToOrganizer`/`DemoteToParticipant`/`RemoveParticipant`) et l'exception `UserNotEventCreatorException` déjà existante — aucune nouvelle exception créée. `GlobalExceptionHandler` complété avec un cas explicite `UserNotEventCreatorException` → 403 (jusqu'ici absorbée par le cas générique `DomainException` → 400, inséré avant ce dernier, même ordre de précédence que `EventNotFoundException`/404).
  - **Piège anticipé grâce à une mémoire déjà actée** (cf. décision du 2026-08-31 sur les FK EventParticipants/EventTasks → Events en `DeleteBehavior.Restrict`, jamais `Cascade`, pour garder la main explicitement sur ce qui est supprimé) : `EventRepository.DeleteAsync` charge et supprime explicitement les `EventParticipant`/`EventTask` de l'événement (via `dbContext.Set<T>().Where(...).ToListAsync()` + `RemoveRange`, PAS `ExecuteDeleteAsync` — non supporté par le provider InMemory utilisé par `Application.Tests`) avant de supprimer l'événement lui-même, sans quoi la suppression aurait échoué en base (contrainte FK) dès qu'un événement a au moins un participant (systématiquement vrai : le créateur est inscrit comme participant dès `Event.Create`).
  - Tests Gherkin/Reqnroll ajoutés dans les trois projets : `Domain.Tests` (créateur autorisé / non autorisé), `Application.Tests` (suppression par le créateur + vérification que l'événement n'est plus consultable ensuite, suppression par un non-créateur, suppression d'un événement inexistant — nouveau step `Given` reformulé « un événement **à supprimer** ... » pour éviter la même collision de step Reqnroll que lors des tâches précédentes), `Api.Tests` (4 scénarios : créateur → 204, inexistant → 404, sans cookie → 401, non-créateur → 403 — ce dernier réutilisant les steps partagés existants en ouvrant une seconde session via `Etant donné une session ouverte via l'API pour "..."` après la création de l'événement par la première, sans step dédié supplémentaire).
  - Vérifié en conditions réelles : `dotnet test` sur les trois projets de tests backend, tous verts (`Domain.Tests` 29 dont les 2 nouveaux, `Application.Tests` 21 dont les 3 nouveaux, `Api.Tests` 20 dont les 4 nouveaux — Docker/Testcontainers disponible et utilisé). `dotnet build` de la solution complète sans avertissement.
- 2026-09-07 — [Lot 2] Invitation d'un participant par email (`POST /api/events/{id}/participants`) : Command `InviteParticipantCommand`/`InviteParticipantCommandHandler` (même pattern `ICommand<TResponse>`/`ICommandDispatcher` que les tâches précédentes du lot), délègue à `Event.InviteParticipant` (déjà présent et testé côté Domain depuis le lot 1, jusqu'ici jamais exercé par aucun Command). Récupération/création de l'utilisateur invité par email selon le même principe qu'à la première connexion (`VerifyMagicLinkCommandHandler` : compte réutilisé si l'email existe déjà, sinon créé à la volée) — cohérent avec le cadrage (§2.1 point 5, "première connexion = inscription"). `InviteParticipantCommandValidator` : `NotEmpty` + `EmailAddress` sur l'email, cohérent avec `RequestMagicLinkCommandValidator` (le Domain — `Email.Create` — reste malgré tout la source de vérité finale du format, cette règle FluentValidation n'étant qu'une garde-fou explicite côté API). **Décisions notables** :
  - **Reprise d'un chantier commencé puis mis de côté** : un commit `wip` (non poussé, jamais mergé) portant déjà une implémentation quasi complète de cette tâche existait sur `main`, antérieur au refactoring de découplage `Infrastructure`/`Domain` du 2026-09-07 (`main` et `refacto_entities` avaient divergé juste avant ce commit). Repris comme référence de conception (forme du Command/Handler/Validator/contrats API, scénarios Gherkin) plutôt que cherry-ické tel quel : son `EventRepository.UpdateAsync` ciblait encore l'ancien mapping direct EF Core → agrégats `Domain`, incompatible avec l'architecture Entity/Mapper actuelle.
  - **Bug EF Core réellement rencontré (et non plus seulement anticipé) en réexécutant ce chantier sur l'architecture actuelle** : `EventRepository.GetByIdAsync`/`UpdateAsync` chargent déjà `Participants`/`Tasks` et `EventMapper.ApplyToEntity` synchronise déjà les collections enfants par comparaison d'Id (mis en place lors du refactoring Entity/Mapper du 2026-09-07) — mais ajouter un nouvel `EventParticipantEntity` à la collection d'un `EventEntity` déjà suivi par le `ChangeTracker` (chargé puis modifié dans le même `DbContext`, cas réel dès qu'une invitation suit un `GetByIdAsync` dans le même scope) fait toujours échouer `SaveChangesAsync` (`DbUpdateConcurrencyException`, reproduit par le test Gherkin `Application.Tests` avant correction). Cause : les Id de `EventParticipantEntity`/`EventTaskEntity` sont générés côté Domain (`Guid.NewGuid()`), mais restaient configurés `ValueGeneratedOnAdd` (comportement par défaut EF Core pour une clé `Guid`) — sans le savoir explicitement, `DetectChanges` ne peut pas distinguer "nouvelle entité au Id déjà défini" d'"entité existante rechargée puis modifiée" quand elle apparaît dans une collection déjà suivie, et la marque à tort `Modified`. **Corrigé différemment de l'ancien commit `wip`** (qui rattachait manuellement en `Added` via le `ChangeTracker`, sur l'ancienne architecture) : `builder.Property(p => p.Id).ValueGeneratedNever()` ajouté dans `EventParticipantConfiguration`/`EventTaskConfiguration` — solution idiomatique EF Core pour une clé entièrement gérée par l'application, qui lève l'ambiguïté à la source. Aucun impact sur le schéma (vérifié via `dotnet ef migrations add`, diff `Up`/`Down` vide, migration supprimée ; seul le model snapshot a été régénéré, comme lors du refactoring du 2026-09-07). Concerne aussi par avance le lot 3 (`AddTask` sur un `Event` chargé, structurellement identique).
  - Pas de vérification d'autorisation par rôle ici non plus, même choix délibéré qu'à `GetEventById`/`UpdateEvent` (cf. notes correspondantes) : la tâche dédiée (« Logique métier : distinction créateur / co-organisateur / participant ») reste le prochain élément du backlog. Le cadrage (§3.2) autorise le créateur **et** les co-organisateurs à inviter — `POST /api/events/{id}/participants` est donc pour l'instant accessible à tout utilisateur authentifié (`[Authorize]` seul). **À re-couvrir cet endpoint à ce moment-là.**
  - Pas d'envoi d'email de notification à la personne invitée : explicitement une tâche séparée du backlog (lot 4, « Notification email : invitation à un événement »), non anticipée ici.
  - Tests Gherkin/Reqnroll ajoutés dans `Application.Tests` (5 scénarios : nouvelle personne, personne ayant déjà un compte, personne déjà invitée, email invalide, événement inexistant) et `Api.Tests` (4 scénarios : invitation réussie → 201, événement inexistant → 404, sans cookie → 401, email invalide → 400).
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement ; `dotnet test` sur `Domain.Tests` (29, inchangé) et `Application.Tests` (26 dont les 5 nouveaux) verts.
    > Bloqué (partiel), résolu le 2026-09-07 : `Api.Tests` a pu être exécuté lors de la tâche suivante (Docker disponible), les 4 scénarios ajoutés ici sont verts — tâche définitivement close.
- 2026-09-07 — [Lot 2] Logique métier : distinction créateur / co-organisateur / participant (règles d'autorisation dans l'agrégat `Event`). Le cadrage (§3.2) n'introduit qu'une seule vraie coupure de droits restant à couvrir au-delà de ce qui existait déjà (créateur seul : suppression/gestion des rôles, déjà en place depuis les tâches précédentes) : créateur **et** co-organisateurs peuvent modifier l'événement et inviter, alors que seuls les participants (tous rôles confondus) peuvent le consulter. Deux nouvelles règles ajoutées dans l'agrégat `Event`, réutilisées telles quelles par les Commands/Query déjà existants (`GetEventById`, `UpdateEvent`, `InviteParticipant`), qui portaient jusqu'ici la note « autorisation non implémentée, à re-couvrir ici » dans leurs entrées de journal respectives (2026-09-04, 2026-09-04, 2026-09-07) :
  - `Event.EnsureCanBeViewedBy(actingUserId)` (consultation) — méthode publique du même type que `EnsureCanBeDeletedBy`, appelée par `GetEventByIdQueryHandler` (désormais injecté avec `ICurrentUserService`) après le chargement de l'agrégat. Nouvelle exception `UserNotEventParticipantException` → 403 (`GlobalExceptionHandler`).
  - `EnsureActingUserIsCreatorOrOrganizer` (modification, invitation) — vérification **par rôle** (`ParticipantRole.Organizer`) plutôt que par comparaison à `CreatedByUserId` comme `EnsureActingUserIsCreator` : le créateur possédant toujours une entrée `EventParticipant` avec `Role = Organizer` (cf. `Event.Create`), une seule vérification de rôle couvre à la fois le créateur et les co-organisateurs, sans dupliquer la logique ni introduire un cas `OR` explicite. `UpdateDetails`/`InviteParticipant` prennent désormais `actingUserId` en premier paramètre (même convention que `PromoteToOrganizer`/`DemoteToParticipant`/`RemoveParticipant`) et l'appliquent avant toute autre validation. Nouvelle exception `UserNotEventOrganizerException` → 403. `UpdateEventCommandHandler`/`InviteParticipantCommandHandler` injectés avec `ICurrentUserService` pour la leur transmettre.
  - Pas de nouvelle règle sur `AssignTask`/`CompleteTask`/etc. : le cadrage limite la portée d'un participant simple sur les tâches ("s'auto-assigner", "cocher ses propres tâches"), hors périmètre de cette tâche (lot 3, CRUD des tâches pas encore commencé).
  - **Piège Reqnroll évité par anticipation** (déjà rencontré à plusieurs reprises dans ce lot, cf. notes du 2026-09-03/2026-09-04) : les nouveaux scénarios `Application.Tests` de `GetEventById`/`UpdateEvent`/`InviteParticipant` avaient besoin d'un même step `Etant donné je change d'utilisateur courant`, déjà présent en tant que binding dédié à `DeleteEventSteps` (seul Command avec une autorisation câblée avant cette tâche) — l'ajouter tel quel dans chacune de ces trois nouvelles classes aurait recréé l'ambiguïté Reqnroll déjà rencontrée sur les steps de session/événement existant. Extrait dans un binding partagé (`Support/CurrentUserSteps.cs` + `Support/CurrentUserContext.cs`, même mécanique de context injection que `SessionContext`/`EventContext` côté `Api.Tests`) — `DeleteEventSteps` adapté pour le consommer au lieu de porter sa propre logique. Nouveau `Support/CurrentUserContextService.cs` (`ICurrentUserService` de test qui lit `CurrentUserContext.UserId` dynamiquement, contrairement à `TestDoubles/FixedCurrentUserService` qui reste utilisé tel quel par `CreateEventSteps`/`GetMyEventsSteps`, non concernés par ce changement d'utilisateur en cours de scénario).
  - Scénarios Gherkin ajoutés : `Domain.Tests` (6 : `EnsureCanBeViewedBy` × 2, `UpdateDetails` × 2, `InviteParticipant` × 2 — `PromoteToOrganizer`/`RemoveParticipant`/etc. déjà couverts), `Application.Tests` (3 : un par Command/Query concerné, utilisateur non participant/non organisateur), `Api.Tests` (3, même pattern que le scénario 403 existant de `Delete.feature` : deuxième session ouverte pour un autre utilisateur avant l'action).
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement ; `dotnet test` sur `Domain.Tests` (35, dont les 6 nouveaux) et `Application.Tests` (29, dont les 3 nouveaux) verts.
    > Bloqué (partiel), résolu le 2026-09-07 : `Api.Tests` a pu être exécuté lors de la tâche suivante (Docker disponible), les 3 scénarios ajoutés ici sont verts — tâche définitivement close.
- 2026-09-07 — [Transverse] Découplage `Infrastructure`/`Domain` en persistance : suppression du mapping EF Core direct sur les agrégats `Domain`, au profit de classes de persistance dédiées suffixées `Entity` (`Infrastructure/Persistence/Entities/`) et d'un mapping explicite Entity ↔ Domain dans les repositories (`Infrastructure/Persistence/Mapping/`). Tâche demandée explicitement par le développeur (pas de tâche de lot dédiée, refactoring transversal touchant toutes les entités `Domain` déjà en place), traitée immédiatement plutôt que différée. **Décision détaillée dans `conventions-code.md` §1.2** (nouvelle note du 2026-09-07) : classes `XxxEntity` anémiques (Email en `string` brute), reconstruction des agrégats `Domain` via des factories `internal static Reconstitute(...)` (`InternalsVisibleTo` `Domain` → `Infrastructure`), `EventRepository.UpdateAsync` resynchronise désormais explicitement les collections enfants (`Participants`/`Tasks`) faute de change tracking EF Core implicite sur l'agrégat. Schéma de base inchangé (vérifié via `dotnet ef migrations add`, diff vide, migration supprimée) — seul le model snapshot EF a été régénéré (référence les nouveaux types CLR). Tests `Application.Tests`/`Api.Tests` ajustés en conséquence (assertions directes sur `UserEntity`/`MagicLinkTokenEntity`/`EventEntity` plutôt que sur les agrégats `Domain`). **Correction du 2026-09-07** : convention initialement implémentée avec le préfixe `Entity` (`EntityUser`, etc.) à la demande du développeur — corrigée en suffixe (`UserEntity`, etc.) suite à une clarification du développeur juste après coup. Vérifié en conditions réelles : `dotnet build` (solution complète) et `dotnet test` (`Domain.Tests` 29, `Application.Tests` 21) tous verts ; `Api.Tests` compile mais non exécuté (Docker non disponible dans cet environnement pour cette tâche).
- 2026-09-07 — [Lot 2] Promotion/rétrogradation d'un participant en co-organisateur, réservée au créateur (`POST /api/events/{id}/participants/{userId}/promote` et `.../demote`) : `PromoteToOrganizerCommand`/`PromoteToOrganizerCommandHandler` et `DemoteToParticipantCommand`/`DemoteToParticipantCommandHandler` (pattern `ICommand`/`ICommandHandler<TCommand>` sans réponse, comme `DeleteEventCommand` — pas de validateur FluentValidation, aucune saisie utilisateur au-delà des `Guid` de route). Délèguent aux méthodes `Event.PromoteToOrganizer`/`Event.DemoteToParticipant`, déjà présentes côté Domain depuis le lot 1 mais jusqu'ici jamais exercées par aucun Command. **Décisions notables** :
  - **Deux Commands distincts plutôt qu'un seul Command générique "changer le rôle"** : le Domain expose deux méthodes séparées avec des règles différentes (`DemoteToParticipant` interdit explicitement de rétrograder le créateur, `PromoteToOrganizer` non) — un seul Command paramétré par un rôle aurait réintroduit une forme de setter générique côté Application, contraire au principe "use cases explicites" (`CLAUDE.md`). Chaque Command correspond 1:1 à une méthode de l'agrégat, même logique que `DeleteEventCommand`/`InviteParticipantCommand`. Côté route, un seul verbe REST par action (`/promote`, `/demote`) plutôt qu'un `PATCH` générique avec un corps `{ "role": ... }` — pas de précédent de route imbriquée type "action" dans ce repo avant `/verify` (auth), mais cohérent avec la séparation déjà faite côté Domain.
  - **Pas de mapping HTTP dédié ajouté pour `ParticipantNotFoundException`/`EventCreatorCannotBeDemotedException`** dans `GlobalExceptionHandler` : ces deux exceptions restent absorbées par le cas générique `DomainException` → 400, comme `ParticipantAlreadyInvitedException` déjà en place — cohérent avec ce précédent plutôt que d'introduire un nouveau cas 404/409 non demandé par la tâche.
  - **Tests Domain manquants comblés au passage** (rule 5) : `PromoteToOrganizer`/`DemoteToParticipant` n'étaient couverts que partiellement dans `Domain.Tests` (`RemoveParticipant` avait ses deux cas d'erreur testés, pas `DemoteToParticipant` malgré une structure similaire — cible = créateur, cible non participante). 5 tests ajoutés dans `EventTests.cs` pour aligner la couverture (`PromoteToOrganizer_TargetNotParticipant_...`, `DemoteToParticipant_ActingUserNotCreator_...`, `DemoteToParticipant_TargetIsCreator_...`, `DemoteToParticipant_TargetNotParticipant_...`, `DemoteToParticipant_ActingUserIsCreator_ChangesRole`).
  - **Piège Reqnroll rencontré une nouvelle fois côté `Application.Tests`** (même famille que les collisions déjà documentées les 2026-09-03/04/07) : le `Given` initialement écrit sous la forme `un événement "..." prévu le "..." au lieu "..." avec un participant invité/co-organisateur "..."` matchait par erreur le pattern (non ancré) du `Given` déjà existant de `GetEventByIdSteps` (`un événement "(.*)" prévu le "(.*)" au lieu "(.*)"`), celui-ci n'exigeant pas explicitement la fin de chaîne — ambiguïté Reqnroll au runtime, pas seulement à la compilation. Résolu en réordonnant le texte (`un événement "..." avec un participant invité "...", prévu le "..." au lieu "..."`) plutôt qu'en reformulant un mot comme fait précédemment pour `UpdateEvent` — le mot-clé `existant` était déjà pris par `UpdateEventSteps` et aurait recréé la même collision. **Nouvelle prise de conscience documentée ici pour la suite** : Reqnroll bucketise aussi les steps par type Given/When/Then au moment du matching (pas seulement au moment de la génération du squelette) — un step enregistré en `[When]` n'est pas retrouvé pour une ligne Gherkin `Et` qui suit un `Etant donné` (donc typée `Given`), même avec un texte strictement identique. Rencontré côté `Api.Tests` : `Et je promeus ce participant en co-organisateur via l'API` (voulant réutiliser le `[When]` de `PromoteToOrganizerSteps`) échouait avec "no matching step definition" alors que le texte correspondait exactement. Corrigé en introduisant un `Given` dédié et distinctement formulé (`Support/PromotedParticipantSteps.cs`, "ce participant est déjà promu co-organisateur via l'API") plutôt que de réutiliser le `[When]` existant — nouveau binding partagé (comme `ExistingEventSteps`/`InvitedParticipantSteps`) car requis à la fois par le scénario "rétrogradation réussie" et "rétrogradation par un non-créateur".
  - `EventContext` (`Api.Tests/Support/`) étendu avec `CreatedByUserId` (rempli par `ExistingEventSteps`, déjà appelé par tous les scénarios qui créent un événement) : nécessaire pour le scénario "rétrogradation du créateur lui-même" côté `Api.Tests`, qui n'avait jusqu'ici aucun moyen de connaître l'id de l'utilisateur courant après connexion (seul le cookie était partagé). Nouveau binding partagé `Support/InvitedParticipantContext.cs`/`InvitedParticipantSteps.cs` (même mécanique), extrait de la logique déjà écrite dans `InviteParticipantSteps` de la tâche précédente pour éviter de la dupliquer.
  - Scénarios Gherkin ajoutés : `Domain.Tests` (+5, voir ci-dessus), `Application.Tests` (4 pour `PromoteToOrganizer` + 4 pour `DemoteToParticipant`, mêmes helpers `CurrentUserContext`/`CurrentUserContextService` que `DeleteEvent`/`InviteParticipant`), `Api.Tests` (4 pour `PromoteToOrganizer` + 5 pour `DemoteToParticipant`, dont le cas "rétrogradation du créateur" → 400).
  - Vérifié en conditions réelles, Docker disponible dans cet environnement pour cette tâche (contrairement aux deux tâches précédentes du 2026-09-07) : `dotnet build` de la solution complète sans avertissement ; `dotnet test` sur les trois projets tous verts — `Domain.Tests` 40 (35 + 5 nouveaux), `Application.Tests` 37 (29 + 8 nouveaux), `Api.Tests` **36 (27 + 9 nouveaux), y compris l'intégralité des scénarios laissés en suspens (`Api.Tests` non exécutés faute de Docker) par les deux tâches précédentes du 2026-09-07 (`InviteParticipant`, autorisation créateur/co-organisateur/participant) — tous confirmés verts au passage, ces deux tâches peuvent donc être considérées comme définitivement closes.**
- 2026-09-08 — [Lot 2] Frontend : page de création d'événement (`/events/new`, `CreateEventPage`) : formulaire titre/date/lieu/description (même style que `RequestMagicLinkForm`), `useCreateEvent` (mutation React Query, invalide `['events','mine']` en cas de succès puis redirige vers `/events`). Lien "Nouvel événement" ajouté sur `EventsDashboardPage` (aucune autre page n'y menait jusqu'ici). Comportement (Vitest/MSW/Gherkin) ajouté dès cette tâche (`CreateEvent.feature`), cohérent avec les décisions précédentes du lot. **Décisions notables** :
  - `eventDate` porté par un simple `<input type="date">` (pas d'heure, cohérent avec l'affichage du tableau de bord qui n'affiche que la date) ; la conversion vers le format attendu par l'API se fait à la frontière (`features/events/api.ts`), pas dans le composant.
  - **Deux bugs réels découverts et corrigés en testant en conditions réelles (rule 5), sans lien avec le formulaire lui-même** : invisibles jusqu'ici car jamais exercés ensemble (tests de comportement mockés au niveau réseau, E2E tournant sur une stack où front et API sont tous deux en HTTP donc "same-site") :
    1. **Cookie de session en `SameSite=Lax`** (`AuthController.Verify`) : jamais corrigé depuis la note laissée en suspens le 2026-09-01 ("nécessitera CORS + `SameSite=None`"). En dev réel, le front (`http://localhost:5173`) et l'API (`https://localhost:7166`) diffèrent de **schéma** (http vs https) donc sont cross-site au sens *schemeful same-site* — `SameSite=Lax` ne couvre que les navigations top-level, pas les appels `fetch`, qui échouaient donc tous en 401 malgré un cookie correctement déposé (vérifié via `curl` : la même requête avec le cookie en pièce jointe fonctionne). Corrigé en `SameSite=None` (déjà `Secure=true`, prérequis pour `None`). **Ce bug préexistait et affectait déjà silencieusement toutes les pages précédentes du lot (tableau de bord inclus)** dès qu'on sortait des tests automatisés pour utiliser l'app réellement dans ce contexte dev précis — non détecté avant faute d'avoir testé ce chemin exact (E2E utilise une stack où API et front sont tous deux en HTTP, donc same-site, ce qui masque le problème).
    2. `CreateEventRequest.EventDate` : une date sans fuseau (ex: `"2026-12-24"`, ce qu'envoyait naïvement le formulaire) est désérialisée par System.Text.Json en `DateTime` de `Kind=Unspecified`, que Npgsql refuse d'écrire dans la colonne `timestamptz` (`ArgumentException`, 500). Corrigé côté frontend (`api.ts`, à la frontière) en forçant l'heure et le "Z" UTC (`${eventDate}T00:00:00.000Z`) plutôt que côté backend, cohérent avec le fait que l'API attend déjà des dates UTC ailleurs (`GET /api/events`) — **point à surveiller pour la future page d'édition d'événement (lot 2, pas encore faite)**, qui devra appliquer la même conversion.
  - Vérifié en conditions réelles, au-delà des suites automatisées (`npm test`, `npm run build`, `dotnet test` sur les trois projets backend, tous verts) : parcours complet dans un vrai navigateur (script Playwright ad hoc, faute de `chromium-cli` disponible ici — même contournement que la vérification manuelle du 2026-09-04) contre la stack de dev réelle (`docker compose` pour Postgres, API lancée localement en `https` comme documenté dans `CLAUDE.md`, `npm run dev`) : connexion par lien magique, navigation vers la page de création, tentative avec titre vide bloquée par la validation HTML5 (`required`), création réussie (`POST /api/events` → 201), redirection vers `/events`, nouvel événement bien affiché (titre, date, lieu) — aucune erreur console.
  > Bloqué (partiel), résolu le 2026-09-08 : la couche non-régression visuelle avait été omise (seule la couche comportement avait été ajoutée) — corrigée ci-dessous, tâche définitivement close.
- 2026-09-08 — [Lot 2] Correction : ajout de la couche non-régression visuelle manquante pour la page de création d'événement (`tests/visual/features/apparence-creation-evenement.feature` + `steps/apparence-creation-evenement.steps.ts`, 2 scénarios × 3 tailles d'écran — formulaire vide, formulaire avec message d'erreur serveur mocké via `page.route`), à la demande explicite du développeur suite à un oubli constaté sur la tâche précédente. **Décisions notables** :
  - Captures de référence générées via l'image Docker Playwright exacte de la CI (`mcr.microsoft.com/playwright:v1.62.1-jammy`, `node_modules` isolé dans un volume Docker nommé), même procédure que celle actée le 2026-09-04 pour le tableau de bord — pas via `npm run test:visual:update` en local (rendu de fonts différent sous Windows).
  - **Définition de "terminé" clarifiée dans `conventions-code.md` §2.3** (nouvelle note du 2026-09-08) pour ne plus reproduire cet oubli : toute tâche frontend ajoutant une page/un composant significatif doit inclure un scénario de non-régression visuelle, au même titre que la couche comportement, avant d'être considérée terminée.
  - **Anomalie préexistante et sans rapport détectée en rejouant toute la suite `test:visual`** : les 2 scénarios `mobile` du tableau de bord (`apparence-tableau-de-bord.feature`, déjà en place depuis le 2026-09-04) échouent désormais contre leurs captures de référence versionnées (~3-4% de pixels, décalage de sous-pixel du texte visible sur le diff — probablement une dérive de rendu de police côté image Docker `v1.62.1-jammy`, pourtant un tag figé). Non lié à cette tâche (page/code non touchés) et non corrigé ici : régénérer ces 2 captures sans regard humain contredirait la décision du 2026-09-03 (validation explicite du développeur avant tout commit de nouvelle référence). **Signalé pour arbitrage** : soit rejouer `update-visual-baselines` et valider le diff, soit investiguer la dérive de rendu si elle s'avère plus large.
  - Vérifié en conditions réelles : suite `test:visual` complète rejouée dans le même conteneur Docker — les 6 nouveaux scénarios (page de création) et les 6 scénarios existants des pages d'authentification passent ; seuls les 2 scénarios mobile du tableau de bord échouent (anomalie ci-dessus, préexistante).
- 2026-09-08 — [Lot 2] Frontend : page de détail d'un événement (`/events/:eventId`, `EventDetailPage`) : infos (titre, date, lieu, description) + liste des participants (nom, email, rôle, badge "Invitation en attente"), avec actions promouvoir/rétrograder co-organisateur pour le créateur. Lien ajouté depuis chaque ligne du tableau de bord (`EventsDashboardPage`) vers le détail correspondant. **Décisions notables** :
  - **Backend étendu au passage** : `GET /api/events/{id}` ne renvoyait jusqu'ici que les champs scalaires de l'événement (vérifié dans le code actuel, la note du 2026-09-03 sur l'absence d'`Include` sur `Participants` était devenue obsolète depuis la tâche d'invitation) — la liste des participants n'était donc exposée nulle part. `GetEventByIdResult` étendu avec `IReadOnlyList<EventParticipantSummary> Participants` (même pattern que `MyEventSummary`/`GetMyEventsResult`), `GetEventByIdQueryHandler` injecté avec `IUserRepository` pour résoudre l'email/displayName de chaque participant (même logique que `InviteParticipantCommandHandler`). Nouveau contrat API dédié `EventDetailResponse` (plutôt que d'étendre `EventResponse`, partagé par `Create`/`Update`, qui n'ont pas besoin de cette liste) — réutilise tel quel `EventParticipantResponse` déjà existant pour chaque participant.
  - **Frontend sans mécanisme d'utilisateur courant jusqu'ici** (`GET /api/auth/me` existe depuis le lot 1 mais n'était consommé par aucune page, point signalé en suspens dans le journal du 2026-09-04) : ajout de `useCurrentUser` (`features/auth/hooks/`, React Query sur `/api/auth/me`), nécessaire ici pour savoir si l'utilisateur affichant la page est le créateur (seul habilité à promouvoir/rétrograder) — les boutons d'action ne sont rendus que si `event.createdByUserId === currentUser.userId`, en plus de l'autorisation déjà appliquée côté API.
  - **Piège de test découvert et corrigé** : les scénarios `Application.Tests` de `GetEventById` faisaient jusqu'ici passer un `CurrentUserContext.UserId` fictif (`Guid.NewGuid()`, sans ligne `User` réelle en base) comme créateur, ce qui suffisait tant que le handler ne résolvait aucun `User`. Devenu un `NullReferenceException` une fois la résolution des participants ajoutée. Corrigé en créant un vrai compte `User` et en l'assignant comme utilisateur courant avant la création de l'événement dans `GetEventByIdSteps` (seule classe concernée). `DeleteEventSteps` (qui appelle aussi `GetEventByIdQuery` pour vérifier la suppression) avait seulement besoin d'enregistrer `IUserRepository` dans son conteneur de test, sans scénario affecté (l'événement n'existe plus au moment de l'appel, l'exception `EventNotFoundException` est levée avant la résolution des participants).
  - **Piège de test frontend découvert** : plusieurs scénarios du nouveau fichier de comportement affichent tous un participant "Ami" — sans démontage explicite entre scénarios (aucun `cleanup()` global enregistré, cf. `src/test/setup.ts`), le DOM du scénario précédent restait présent et rendait `findByText('Ami')` ambigu. Résolu en ajoutant `cleanup()` (testing-library) dans le `AfterEachScenario` de ce fichier uniquement — les fichiers de comportement précédents n'avaient jamais recoupé de texte entre scénarios et n'avaient donc jamais révélé ce point.
  - **Anomalie préexistante et sans rapport détectée en vérifiant la suite `test:visual`** : le dossier de captures de référence de la page de création d'événement (`apparence-creation-evenement.feature.spec.js/`, ajouté le 2026-09-08) n'a en réalité jamais été suivi par Git (`git status` le montre encore non indexé) — probablement un oubli de `git add` lors de la tâche précédente. Concrètement, la CI (`frontend-visual-tests`) tourne donc sans ces fichiers depuis leur création et échoue très probablement dessus (`snapshot doesn't exist`) à chaque run depuis. Non corrigé ici (pas de `git add`/commit fait par l'agent sans validation explicite du développeur, cf. règle établie le 2026-09-03 : la validation d'une capture de référence reste un geste humain explicite) — **signalé pour action du développeur** : `git add client/tests/visual/__screenshots__/apparence-creation-evenement.feature.spec.js/` avant le prochain commit.
  - Vérifié en conditions réelles : `dotnet test` (3 projets, tous verts : `Domain.Tests` 40 inchangé, `Application.Tests` 38 dont les 4 nouveaux scénarios `GetEventById`, `Api.Tests` 37 dont le nouveau scénario "avec ses participants") ; `npm run build` et `npm test` (39 scénarios, dont les 4 nouveaux de `EventDetail.feature`) ; suite `test:visual` complète rejouée dans le conteneur Docker CI (les 6 nouveaux scénarios de détail passent, seuls les 2 scénarios mobile du tableau de bord échouent — anomalie déjà connue et sans rapport) ; `npm run test:e2e` (3 scénarios existants, non-régression confirmée sur le tableau de bord malgré le changement de balisage — chaque ligne est désormais un lien vers le détail).
- 2026-09-08 — [Lot 2] Frontend : formulaire d'invitation de participants, intégré à `EventDetailPage` (pas de page dédiée : c'est là que vit déjà la liste des participants). Nouveau composant `InviteParticipantForm` (email + bouton, même style que `RequestMagicLinkForm`/`CreateEventPage`) + `useInviteParticipant` (mutation React Query, invalide `['events', eventId]` en cas de succès — réutilise `POST /api/events/{id}/participants`, déjà en place côté backend depuis le 2026-09-07). **Toutes les tâches Must have du lot 2 sont désormais cochées — lot prêt pour la review développeur avant de démarrer le lot 3.** **Décisions notables** :
  - **Formulaire affiché au créateur et aux co-organisateurs, pas seulement au créateur** : contrairement à promouvoir/rétrograder (réservé au créateur), le backend autorise l'invitation au créateur **et** aux co-organisateurs (`EnsureActingUserIsCreatorOrOrganizer`, cf. journal du 2026-09-07). Condition d'affichage `isCreator || currentParticipant?.role === 'Organizer'` (`currentParticipant` = l'entrée de `event.participants` correspondant à l'utilisateur courant), pas seulement `isCreator` comme pour les boutons de promotion/rétrogradation juste au-dessus dans le même composant.
  - Pas de validation FluentValidation dupliquée côté formulaire au-delà de `type="email"`/`required` (HTML5) : cohérent avec `RequestMagicLinkForm`, le message d'erreur backend (déjà spécifique : email invalide, déjà invité) est affiché tel quel.
  - **Anomalie découverte en préparant la couche non-régression visuelle** : le journal du 2026-09-08 précédent (page de détail) affirmait que les 6 captures de référence de `apparence-detail-evenement.feature` avaient été générées et passaient en Docker — en réalité, aucune n'a jamais été commitée (`git log`/`git status` : dossier `tests/visual/__screenshots__/apparence-detail-evenement.feature.spec.js/` absent de l'historique, y compris avant les changements de cette tâche). Probablement perdues par erreur (même famille que l'oubli de `git add` déjà documenté juste au-dessus pour `apparence-creation-evenement`) sans qu'un `git status` n'ait été relancé pour le vérifier à l'époque. Les 9 captures (3 scénarios dont les 2 déjà existants × 3 tailles) régénérées ici avec ce correctif.
  - Captures de référence générées via l'image Docker Playwright exacte de la CI (`mcr.microsoft.com/playwright:v1.62.1-jammy`, `node_modules` isolé dans un volume Docker nommé puis supprimé après usage), même procédure que les fois précédentes — **restreinte au seul fichier `apparence-detail-evenement.feature.spec.js`** (`playwright test <chemin> --update-snapshots=all`) plutôt qu'une régénération globale, pour ne pas re-couvrir sans regard humain l'anomalie de rendu des 2 captures mobile du tableau de bord signalée le 2026-09-08 (toujours non arbitrée). Suite `test:visual` complète (27 scénarios) rejouée ensuite sans `--update-snapshots` pour confirmer l'absence de régression ailleurs : tout est vert, y compris ces 2 captures mobile du tableau de bord — l'anomalie de rendu signalée ne s'est pas reproduite ici, laissée telle quelle (non ré-investiguée, hors périmètre de cette tâche).
    > Ces 9 nouvelles captures ne sont pas commitées (geste de validation humaine explicite, cf. décision du 2026-09-03) : `git add client/tests/visual/__screenshots__/apparence-detail-evenement.feature.spec.js/` après relecture visuelle par le développeur.
  - Vérifié en conditions réelles : `npm run build` et `npm test` (51 scénarios, dont les 5 nouveaux de `EventDetail.feature` — invitation réussie, email déjà invité, formulaire visible pour un co-organisateur non créateur, formulaire masqué pour un simple participant) tous verts ; suite `test:visual` complète (27 scénarios) verte dans le conteneur Docker CI. Pas de changement backend, `dotnet test` non relancé.
- 2026-09-08 — [Lot 3] Création d'une tâche (`POST /api/events/{id}/tasks`) : Command `CreateTaskCommand`/`CreateTaskCommandHandler` (même pattern `ICommand<TResponse>`/`ICommandDispatcher` que les Commands du lot 2), délègue à `Event.AddTask` (déjà présent côté Domain depuis le lot 1, jusqu'ici jamais exercé par aucun Command — le piège EF Core `ValueGeneratedOnAdd`/`ChangeTracker` sur les collections enfants, rencontré et corrigé lors de l'invitation de participant le 2026-09-07, avait déjà été anticipé pour ce cas précis via `EventTaskConfiguration.ValueGeneratedNever()`, donc pas reproduit ici). **Décisions notables** :
  - **Point d'architecture non couvert explicitement par `conventions-code.md`, traité par cohérence avec l'existant, signalé pour validation puis corrigé suite à la réponse du développeur** : `Event.AddTask` ne portait jusqu'ici aucun contrôle de droits. Interprétation initiale par analogie avec `UpdateDetails`/`InviteParticipant` (créateur/co-organisateur seulement, cadrage §3.2 "un co-organisateur peut... gérer les tâches") — **corrigée** suite à la clarification explicite du développeur : l'ajout d'une tâche est ouvert à **tout participant** de l'événement, quel que soit son rôle (cohérent avec le cas d'usage de référence du cadrage §1.3, "chacun s'assigne ou se voit assigner des éléments"). `AddTask` prend un paramètre `actingUserId` et appelle désormais `EnsureActingUserIsParticipant(actingUserId)` — nouveau helper privé, extrait de la logique déjà présente dans `EnsureCanBeViewedBy` (même règle : appartenir à `_participants`, quel que soit le rôle), réutilisé par les deux méthodes plutôt que dupliqué. Exception `UserNotEventParticipantException` (déjà existante et déjà mappée en 403 dans `GlobalExceptionHandler`, réutilisée telle quelle par `EnsureCanBeViewedBy`) plutôt que `UserNotEventOrganizerException`.
  - **Catégorie exposée en `string` à la frontière API** (`CreateTaskRequest.Category`/`CreateTaskCommand.Category`), pas l'enum `TaskCategory` directement : aucun `JsonStringEnumConverter` n'est configuré globalement (`Program.cs`), et tous les enums exposés jusqu'ici (`EventStatus`, `ParticipantRole`) ne traversent la frontière API qu'en `string` (jamais en entrée il est vrai, mais toujours en sortie) — cohérent de garder la même convention plutôt que de transmettre un entier brut ou d'introduire une config JSON globale pour ce seul besoin. Validation de la valeur via `CreateTaskCommandValidator` (`Enum.TryParse<TaskCategory>`), conversion en enum Domain faite dans le handler seulement après validation.
  - Pas de nouvelle exception ni de nouveau cas dans `GlobalExceptionHandler` : `EventNotFoundException` (404), `UserNotEventOrganizerException` (403) et `ValidationException`/`DomainException` (400, dont `EventTaskTitleEmptyException`) étaient déjà tous mappés.
  - `IEventRepository`/`EventRepository`/`EventMapper` inchangés : `UpdateAsync` recharge déjà `Tasks` et `EventMapper.ApplyToEntity` synchronise déjà cette collection (mécanisme générique mis en place lors du découplage Entity/Mapper du 2026-09-07), la nouvelle tâche ajoutée par `AddTask` est donc automatiquement persistée sans code supplémentaire.
  - Tests Gherkin/Reqnroll ajoutés dans les trois projets, même pattern que `InviteParticipant`/`PromoteToOrganizer` (steps partagés `AuthenticatedSessionSteps`/`ExistingEventSteps`/`InvitedParticipantSteps`/`SessionContext`/`EventContext` côté `Api.Tests`, `CurrentUserContext`/`CurrentUserSteps` côté `Application.Tests`) : `Domain.Tests` (+2 : ajout refusé pour un non-participant, ajout autorisé pour un participant simple non organisateur), `Application.Tests` (6 : créateur, participant simple invité — nouveau `Given`/step dédié pour changer d'utilisateur courant vers ce participant, formulé différemment de `je change d'utilisateur courant` pour éviter la collision de pattern non ancré déjà rencontrée à plusieurs reprises dans ce projet —, titre vide → erreur de validation, catégorie invalide, événement inexistant, autorisation refusée pour un non-participant), `Api.Tests` (6 : 201 par le créateur, 201 par un participant simple invité via sa propre session, 404, 401, 400 catégorie invalide, 403 pour un non-participant).
  - **Piège évité en écrivant le scénario "titre vide"** : `CreateTaskCommandValidator.Title.NotEmpty()` catégorise déjà un titre uniquement composé d'espaces comme invalide (comportement par défaut de FluentValidation), donc ce cas est intercepté par la validation (`ValidationException`) avant même d'atteindre `Event.AddTask`/`EventTaskTitleEmptyException` — cohérent avec le choix déjà fait pour `CreateEvent` (le scénario `Application.Tests` équivalent y teste aussi une erreur de validation, pas `EventTitleEmptyException`, qui elle n'est couverte qu'au niveau `Domain.Tests`).
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement ; `dotnet test` sur les trois projets tous verts — `Domain.Tests` 43 (40 + 3 nets : +4 ajoutés puis -1 test devenu obsolète retiré lors de la correction de la règle d'autorisation), `Application.Tests` 44 (38 + 6 nouveaux), `Api.Tests` 43 (37 + 6 nouveaux), Docker/Testcontainers disponible et utilisé. Aucune migration EF nécessaire (aucune configuration `Infrastructure` modifiée). Vérification uniquement backend (aucune page frontend ne consomme encore cet endpoint — prévu plus bas dans ce lot).
- 2026-09-08 — [Lot 3] Assignation d'une tâche à un participant (`POST /api/events/{id}/tasks/{taskId}/assign/{userId}`) : Command `AssignTaskCommand`/`AssignTaskCommandHandler` (pattern `ICommand`/`ICommandHandler<TCommand>` sans réponse, comme `PromoteToOrganizerCommand`/`DemoteToParticipantCommand`), délègue à `Event.AssignTask` — déjà présent côté Domain depuis le lot 1 mais **sans aucun contrôle d'autorisation** jusqu'ici (seule la règle "la cible doit être participante" existait). Route au même format que `/promote`/`/demote` (verbe REST en dernier segment, identifiants dans l'URL, pas de corps de requête). **Décisions notables** :
  - **Règle d'autorisation manquante ajoutée, directement déduite du cadrage (§3.2)** sans repasser par un cycle de signalement/validation : "un participant peut s'auto-assigner des tâches" (simple participant) vs "un co-organisateur peut... gérer les tâches" (implique assigner à n'importe quel participant) — règle déjà explicite dans le cadrage, contrairement au point d'architecture non couvert rencontré lors de `AddTask` (2026-09-08, plus haut) qui avait nécessité une clarification du développeur. `Event.AssignTask` prend désormais `actingUserId` en premier paramètre (même convention que `UpdateDetails`/`InviteParticipant`/`AddTask`) : vérifie d'abord que l'acteur est participant (`EnsureActingUserIsParticipant`, réutilisée), puis que la cible est participante (`TaskAssigneeNotParticipantException`, inchangée), puis — seulement si la cible diffère de l'acteur — que l'acteur est créateur ou co-organisateur. Nouvelle exception `ParticipantCannotAssignTaskToOthersException` → 403 (`GlobalExceptionHandler`, même famille que `UserNotEventOrganizerException`/`UserNotEventParticipantException`, cohérent avec la règle explicite de `conventions-code.md` §3.2 "403 si droits insuffisants").
  - **Refactoring mineur côté Domain à l'occasion de ce changement** : extraction d'un helper booléen privé `IsCreatorOrOrganizer(userId)` à partir de la logique déjà présente dans `EnsureActingUserIsCreatorOrOrganizer` (qui l'utilise désormais aussi), pour éviter de dupliquer cette vérification entre l'exigence "lève une exception" (`UpdateDetails`/`InviteParticipant`) et le nouveau besoin "renvoie un booléen" (`AssignTask`, qui ne doit lever l'exception d'autorisation que dans le cas précis où la cible diffère de l'acteur).
  - **Changement de signature d'une méthode Domain déjà existante** (`Event.AssignTask(taskId, userId)` → `AssignTask(actingUserId, taskId, userId)`) : aucun appelant existant en dehors des tests (l'endpoint n'existait pas avant cette tâche), migration sans risque. Exemple cité dans `conventions-code.md` §1.2 mis à jour en conséquence.
  - Tests Gherkin/Reqnroll ajoutés dans les trois projets, même pattern que les tâches précédentes du lot (`Domain.Tests` : 5 scénarios remplaçant les 2 tests existants devenus obsolètes avec l'ancienne signature ; `Application.Tests` : 6 scénarios, nouveaux steps locaux à `AssignTaskSteps` pour éviter toute collision de step Reqnroll avec `CreateTaskSteps`/`GetEventByIdSteps` déjà en place — piège désormais bien identifié dans ce projet, cf. notes des 2026-09-03/04/07 ; `Api.Tests` : 6 scénarios, nouveaux steps locaux `AssignTaskSteps` pour la création de tâche/second participant via l'API, faute de contexte partagé existant portant l'id de tâche créée).
  - Vérifié en conditions réelles : `dotnet build` des projets `Domain`/`Api` (couvrant `Application`/`Infrastructure`) sans avertissement ; `dotnet test` sur les trois projets tous verts — `Domain.Tests` 46 (43 + 3 nets : 5 nouveaux scénarios remplaçant les 2 existants), `Application.Tests` 50 (44 + 6 nouveaux), `Api.Tests` 49 (43 + 6 nouveaux), Docker/Testcontainers disponible et utilisé. Aucune migration EF nécessaire. Vérification uniquement backend (aucune page frontend ne consomme encore cet endpoint).
- 2026-09-08 — [Lot 3] Suppression d'une tâche (`DELETE /api/events/{id}/tasks/{taskId}`) : `DeleteTaskCommand`/`DeleteTaskCommandHandler` (même pattern `ICommand`/`ICommandHandler<TCommand>` sans réponse que `CompleteTask`/`ReopenTask`), délègue à `Event.RemoveTask` — déjà présent côté Domain depuis le lot 1 (`_tasks.Remove(GetTask(taskId))`) mais **sans aucun paramètre ni contrôle d'autorisation**, jamais exercé par aucun Command jusqu'ici (seul un test Domain unitaire l'appelait, sur le cas "tâche introuvable"). **Décisions notables** :
  - **Règle d'autorisation non couverte par le cadrage, signalée pour validation avant traitement** (règle 7) : contrairement à `AssignTask`/`CompleteTask` (règle déduite explicitement du cadrage §3.2), rien n'y précise qui peut supprimer une tâche. Trois options proposées au développeur ; **retenue : créateur de l'événement, co-organisateur, **ou** participant qui a créé la tâche lui-même** (pas seulement créateur/co-organisateur). Ce dernier cas nécessite de savoir qui a créé chaque tâche, information absente jusqu'ici : **changement de modèle** — `EventTask` gagne une propriété `CreatedByUserId` (non nullable, renseignée par `Event.AddTask` avec l'`actingUserId`), migration EF `AddEventTaskCreatedByUserId` (colonne `NOT NULL` + FK `Restrict` vers `Users`, même convention que `AssignedToUserId`/`Event.CreatedByUserId`). `Event.RemoveTask(actingUserId, taskId)` délègue à un nouveau helper privé `EnsureActingUserCanDeleteTask` (même forme que `EnsureActingUserCanToggleTaskDone` : participant + (créateur de la tâche OU créateur/co-organisateur de l'événement)) et lève la nouvelle exception `ParticipantCannotDeleteOthersTaskException` → 403 (`GlobalExceptionHandler`, même famille que `ParticipantCannotToggleOthersTaskException`).
  - **Bug préexistant découvert et corrigé, bloquant pour cette tâche** : `EventRepository.UpdateAsync`/`EventMapper.ApplyToEntity` ne géraient jusqu'ici aucun retrait d'enfant des collections `Participants`/`Tasks` d'un agrégat (aucune fonctionnalité précédente ne le faisait — `UnassignTask` existe mais n'est appelée par aucun Command). `SyncChildren` se contentait de retirer l'entité de la `List<T>` en mémoire sans la supprimer explicitement du `DbContext` ; EF Core lève alors `InvalidOperationException` ("relationship severed... required") car les FK `EventTasks`/`EventParticipants` → `Events` sont en `Restrict` (pas de cascade DB, cf. mémoire `[[feedback_no_cascade_delete]]`), donc EF ne peut ni mettre la FK à `null` ni supprimer la ligne de lui-même. Corrigé en faisant retourner par `SyncChildren`/`ApplyToEntity` la liste des entités enfants retirées, supprimées explicitement par l'appelant (`EventRepository.UpdateAsync` : `dbContext.RemoveRange(removedChildren)`), même principe que `DeleteAsync` (suppression explicite des enfants avant le parent, déjà commenté dans ce fichier). Détecté grâce aux tests `Application.Tests` (échec réel contre le fournisseur InMemory, pas un artefact de test) puis reconfirmé contre un vrai Postgres via `Api.Tests`.
  - Tests ajoutés dans les trois projets : `Domain.Tests` (+4, remplaçant le test existant `RemoveTask_UnknownTaskId_...` désormais signé différemment : autorisation refusée pour un non-participant, suppression par le créateur de la tâche lui-même, suppression par le créateur/co-organisateur d'une tâche créée par un autre participant, refus pour un participant simple ni créateur de la tâche ni organisateur), `Application.Tests` (5 scénarios Gherkin, steps locaux à `DeleteTaskSteps` — Then reformulés "la suppression de la tâche ..." plutôt que "la suppression ..." pour éviter la collision déjà rencontrée avec `DeleteEventSteps`, même piège que les tâches précédentes du lot), `Api.Tests` (5 scénarios Gherkin, réutilisant les steps partagés `AuthenticatedSessionSteps`/`ExistingEventSteps`/`InvitedParticipantSteps`/`ExistingTaskSteps`, statuts 204/403/404/401).
  - **Anomalie préexistante et sans rapport détectée en faisant tourner `Api.Tests` plusieurs fois de suite** : la suite complète (64 scénarios) échoue de façon intermittente, un scénario différent et sans lien avec cette tâche à chaque run (`GetById`, `Update`, `CreateTask`, `InviteParticipant`, `CurrentUser`...) — reproduit également en excluant `DeleteTask` du filtre, donc confirmé préexistant. Chaque suite de scénarios prise isolément (`--filter`) passe systématiquement, y compris `DeleteTask`. Recoupe une anomalie déjà notée le 2026-09-08 (tâche précédente, ci-dessous) comme "contention ponctuelle" isolée — en réalité un pattern systématique, pas un one-off. **Non corrigé ici** (hors périmètre, infrastructure de test partagée par tout le lot) : ajouté comme tâche de suivi dans le lot 4 (`Revue globale des tests`, ci-dessus).
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement ; `dotnet test` sur les trois projets tous verts en isolation — `Domain.Tests` 56 (52 + 4 nets), `Application.Tests` 65 (60 + 5 nouveaux), `Api.Tests` 64 (59 + 5 nouveaux), Docker/Testcontainers disponible et utilisé, `dotnet ef migrations add AddEventTaskCreatedByUserId` appliquée sans erreur. Vérification uniquement backend (aucune page frontend ne consomme encore cet endpoint).
- 2026-09-08 — [Lot 3] Marquer une tâche comme faite/non faite (`POST /api/events/{id}/tasks/{taskId}/complete` et `.../reopen`) : `CompleteTaskCommand`/`CompleteTaskCommandHandler` et `ReopenTaskCommand`/`ReopenTaskCommandHandler` (pattern `ICommand`/`ICommandHandler<TCommand>` sans réponse, comme `AssignTask`/`PromoteToOrganizer`/`DemoteToParticipant`). Le socle Domain (`EventTask.MarkDone`/`MarkNotDone`, `Event.CompleteTask`/`ReopenTask`) existait déjà depuis le lot 1 mais n'était jusqu'ici jamais exercé par aucun Command. **Décisions notables** :
  - **Règle d'autorisation directement déduite du cadrage (§3.2)**, comme pour `AssignTask` (2026-09-08 ci-dessus) : "un co-organisateur peut... gérer les tâches" (implique cocher/décocher n'importe quelle tâche) vs "un participant peut... cocher **ses propres** tâches comme faites" (implique : seulement celles qui lui sont assignées). `Event.CompleteTask`/`ReopenTask` prennent désormais `actingUserId` en premier paramètre (même convention que `AssignTask`) et délèguent à un nouveau helper privé commun `EnsureActingUserCanToggleTaskDone` (vérifie la participation puis, si la tâche n'est pas assignée à l'acteur, exige créateur/co-organisateur). Nouvelle exception `ParticipantCannotToggleOthersTaskException` → 403 (`GlobalExceptionHandler`, même famille que `ParticipantCannotAssignTaskToOthersException`). Le cadrage ne précisant la règle explicitement que pour le marquage "faite", la même contrainte a été appliquée par symétrie au démarquage ("non faite"), plutôt que de laisser un participant simple rouvrir librement n'importe quelle tâche.
  - **Reprise d'un chantier commencé puis interrompu sans être commité** : des fichiers générés par Reqnroll (`CompleteTask.feature.cs`/`ReopenTask.feature.cs`, code-behind gitignové `*.feature.cs`) subsistaient dans `Application.Tests`/`Api.Tests` sans les `.feature`/`Steps.cs` correspondants (jamais committés, `git status` ne portait aucune trace) — probablement un essai antérieur de cette même tâche resté local. Plutôt que d'ignorer cette trace, son contenu (noms de fonctionnalité/scénarios, formulation exacte des steps Gherkin) a été extrait de ces fichiers générés et reconstruit à l'identique dans les nouveaux `.feature`/`Steps.cs`, jugé plus abouti qu'une reconception isolée (ex: distinction explicite "tâche assignée" vs "tâche non assignée" dans le `Given`, step combiné "un autre participant a rejoint cet événement et devient l'utilisateur courant").
  - **Api.Tests seulement** : extraction de deux nouveaux steps `Given` partagés dans `Support/` (`ExistingTaskSteps` pour "une tâche ... ajoutée à cet événement via l'API", `AssignedTaskSteps` pour "cette tâche assignée à ce participant via l'API", avec un nouveau `TaskContext` porteur de l'id de tâche) — nécessaire car ce même besoin de setup est désormais partagé par trois fonctionnalités (`AssignTask`, `CompleteTask`, `ReopenTask`), contrairement à la tâche `AssignTask` (2026-09-08) où il n'était utile qu'une fois. `AssignTaskSteps` (`Api.Tests`) adapté pour consommer `TaskContext` au lieu de sa propre logique locale de création de tâche, plutôt que de dupliquer ce step (même précédent que `ExistingEventSteps`/`InvitedParticipantSteps`, cf. 2026-09-03/04). Côté `Application.Tests`, chaque nouveau step Gherkin a en revanche été reformulé localement (`CompleteTaskSteps`/`ReopenTaskSteps` distincts, ex. "avec une tâche assignée" vs "avec une tâche non assignée" vs "avec une tâche assignée déjà marquée comme faite") pour éviter toute collision de step Reqnroll, cohérent avec l'approche déjà retenue dans ce projet (pas d'extraction de contexte partagé côté `Application.Tests`, cf. `UpdateEvent`/`DemoteToParticipant`).
  - Tests ajoutés dans les trois projets : `Domain.Tests` (6 nouveaux scénarios remplaçant le test `CompleteTask` existant, désormais insuffisant avec la nouvelle signature autorisante), `Application.Tests` (10 scénarios Gherkin, 5 par fonctionnalité), `Api.Tests` (10 scénarios Gherkin, 5 par fonctionnalité, statuts 204/403/404/401).
- 2026-09-08 — [Lot 3] Configuration SignalR : Hub `EventHub`, groupement des connexions par `EventId`. `EventHub` (`EventCo.Infrastructure/Realtime/`, `[Authorize]`) expose `JoinEvent(eventId)`/`LeaveEvent(eventId)`, qui ajoutent/retirent la connexion courante d'un groupe SignalR nommé `event-{eventId}` — un groupe par événement plutôt qu'une room implicite par connexion, pour permettre à un même utilisateur de suivre plusieurs événements simultanément (plusieurs onglets). `JoinEvent` réutilise `Event.EnsureCanBeViewedBy` (même règle que `GetEventByIdQuery` : seuls les participants peuvent rejoindre) ; toute `DomainException` levée (événement introuvable ou utilisateur non participant) est convertie en `HubException` pour que le message reste exploitable côté client au lieu du message générique renvoyé par SignalR pour une exception non gérée. Endpoint mappé sur `/hubs/events` (`Program.cs`), passe par le même pipeline CORS/cookie de session que le reste de l'API (aucune configuration supplémentaire nécessaire : SignalR lit `Context.User`, peuplé par `SessionAuthenticationHandler` existant). **Décisions notables** :
  - Hub placé dans `EventCo.Infrastructure` (`Realtime/`), conformément à la structure de projet définie dans `conventions-code.md` §1.1 ("Infrastructure : ... SignalR ..."), bien que le type `Hub` nécessite normalement le SDK Web. Rendu possible via le package `Microsoft.AspNetCore.SignalR` (dernière version publiée : `1.2.12`, qui date de l'ère ASP.NET Core 2.1 — la ligne de package a cessé d'évoluer après le passage de SignalR dans le framework partagé à partir de .NET Core 3.0, mais reste le mécanisme officiellement supporté pour référencer les types SignalR côté serveur depuis une classlib non-Web). Vérifié qu'aucun conflit d'assembly ne se produit avec le framework partagé net10 de l'hôte (`EventCo.Api`, SDK Web) : `dotnet publish` de l'Api ne produit aucun avertissement de type NETSDK1071/downgrade et aucun assembly `Microsoft.AspNetCore.{Http,Routing,Authentication,SignalR}*` de l'ancien package ne se retrouve dans la sortie publiée (résolution de conflit du SDK en faveur du framework partagé, comme attendu pour ce type de package de référence).
  - `services.AddSignalR()` ajouté dans `EventCo.Infrastructure/DependencyInjection.cs` (`AddInfrastructure`), plutôt que directement dans `Program.cs`, cohérent avec le reste de la configuration d'infrastructure déjà centralisée à cet endroit (DbContext, repositories, envoi d'email...).
  - Tests Gherkin/Reqnroll ajoutés dans `Api.Tests` uniquement (`Events/JoinEventHub/`, 3 scénarios : participant rejoint le groupe avec succès, utilisateur non participant rejeté avec une erreur d'autorisation, connexion sans cookie de session refusée) — pas de couverture `Domain.Tests`/`Application.Tests` pertinente ici, cette tâche ne portant sur aucun use case CQRS ni règle de domaine nouvelle (réutilisation telle quelle de `Event.EnsureCanBeViewedBy`). Connexion du client `Microsoft.AspNetCore.SignalR.Client` (ajouté à `Api.Tests`) branchée sur le `WebSocketClient` in-memory de `TestServer` (`server.CreateWebSocketClient()`) avec négociation HTTP désactivée (`SkipNegotiation = true` + transport WebSockets forcé), TestServer ne supportant pas de vrai socket TCP ; cookie de session propagé via `WebSocketClient.ConfigureRequest`.
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement ; `dotnet test` sur les trois projets tous verts — `Domain.Tests` 56 (inchangé), `Application.Tests` 65 (inchangé), `Api.Tests` 67 (64 + 3 nouveaux), Docker/Testcontainers disponible et utilisé ; `dotnet publish` de `EventCo.Api` réussi sans avertissement. Aucune migration EF nécessaire. Le prochain point du lot ("Diffusion temps réel des événements de tâches") reste à traiter séparément : `EventHub` ne fait ici que le socle de connexion/groupement, aucune diffusion de message n'est encore émise vers les groupes.
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement ; `dotnet test` sur les trois projets tous verts — `Domain.Tests` 52 (46 + 6 nets), `Application.Tests` 60 (50 + 10 nouveaux), `Api.Tests` 59 (49 + 10 nouveaux), Docker/Testcontainers disponible et utilisé (un premier run complet de `Api.Tests` a rencontré un échec isolé, sans rapport avec cette tâche, sur un scénario `CreateTask` préexistant — non reproductible en isolant ce scénario ni en relançant la suite complète, vraisemblablement une contention ponctuelle sur la boîte Mailpit partagée entre scénarios exécutés en parallèle). Aucune migration EF nécessaire. Vérification uniquement backend (aucune page frontend ne consomme encore ces endpoints).
- 2026-09-08 — [Lot 3] Diffusion temps réel des événements de tâches (création, assignation, statut, suppression) vers le groupe SignalR concerné, suite du socle posé par la tâche précédente (`EventHub`, ci-dessus). Nouveau port `ITaskRealtimeNotifier` (`EventCo.Application/Common/Interfaces/`, quatre méthodes `NotifyTaskCreated`/`NotifyTaskAssigned`/`NotifyTaskStatusChanged`/`NotifyTaskDeleted`, DTOs dédiés `TaskRealtimeDto`/`TaskDeletedRealtimeDto`), implémenté côté `Infrastructure` (`Realtime/TaskRealtimeNotifier.cs`, via `IHubContext<EventHub>`, diffuse au groupe `EventHub.GroupName(eventId)` avec les noms de méthode client `TaskCreated`/`TaskAssigned`/`TaskStatusChanged`/`TaskDeleted`) et injecté dans les 5 handlers de tâches (`CreateTask`/`AssignTask`/`CompleteTask`/`ReopenTask`/`DeleteTask`), qui appellent la notification juste après `eventRepository.UpdateAsync`. **Décisions notables** :
  - **Point d'architecture non couvert explicitement par `conventions-code.md`, traité par cohérence avec l'existant plutôt que par les Domain Events évoqués en §1.2 (option explicitement qualifiée d'optionnelle) — signalé ici pour validation** : les handlers dépendent directement du port `ITaskRealtimeNotifier`, exactement comme ils dépendent déjà de `IEventRepository`/`IDateTimeProvider`/`IEmailSender` ; pas de bus de Domain Events introduit pour ce seul besoin (aurait ajouté une couche d'indirection — enregistrement/dispatch des handlers d'événements — sans bénéfice concret ici, un seul type d'abonné existant pour l'instant). Cohérent avec la remarque déjà consignée dans le journal du Hub (2026-09-08, ci-dessus : "reste à traiter séparément").
  - **Tâche manquante ajoutée au périmètre de ce point du backlog** (rule 5) : la suppression de tâche (`DeleteTask`) n'était pas listée dans le parenthétique d'origine ("création, assignation, statut") alors qu'elle existe déjà comme Command depuis le 2026-09-08 — sans sa diffusion, la future liste de tâches réactive (frontend, plus bas dans ce lot) resterait incohérente après une suppression tant qu'aucun rechargement manuel n'est fait. Ajoutée au libellé de la case ci-dessus et traitée avec le reste (`NotifyTaskDeleted`, DTO `TaskDeletedRealtimeDto(EventId, TaskId)` — pas de `TaskRealtimeDto` complet ici, la tâche n'existant plus après suppression).
  - `TaskRealtimeDto`/`TaskDeletedRealtimeDto` volontairement placés dans `Application/Common/Interfaces/` (à côté du port, pas dans `Infrastructure`) : ce sont les DTOs du contrat du port, sérialisés tels quels par SignalR (`IClientProxy.SendAsync`) — les garder côté `Application` évite tout accès à un type `internal` d'`Infrastructure` depuis les tests `Api.Tests` (le client SignalR de test désérialise directement `TaskRealtimeDto`/`TaskDeletedRealtimeDto` reçus sur le hub).
  - `TaskRealtimeNotifier` enregistré en singleton (`services.AddSingleton<ITaskRealtimeNotifier, TaskRealtimeNotifier>()`), cohérent avec `IHubContext<EventHub>` qui est lui-même un singleton fourni par `AddSignalR()` — pas de dépendance scope-per-request à porter.
  - **Tests `Application.Tests`** : nouveau double de test `RecordingTaskRealtimeNotifier` (`TestDoubles/`, même famille que `RecordingEmailSender`), enregistré dans les 5 classes `*Steps` concernées (`CreateTask`/`AssignTask`/`CompleteTask`/`ReopenTask`/`DeleteTask`), un `Then` ajouté par scénario nominal de chaque feature vérifiant le contenu de la notification enregistrée. **Piège rencontré** : le premier libellé de step choisi ("une notification temps réel de changement de statut est diffusée") était identique entre `CompleteTaskSteps`/`ReopenTaskSteps` → `Reqnroll.AmbiguousBindingException` (les steps sont liés globalement au niveau de l'assembly de tests, pas par fichier `.feature`, piège de la même famille que ceux déjà documentés en `conventions-code.md` §1.4 avec les collisions `je change d'utilisateur courant`/etc.) — reformulé distinctement par fonctionnalité ("... tâche marquée comme faite ..." / "... comme non faite ..."). Second piège : le `Given` de `ReopenTask` marque déjà la tâche comme faite (`CompleteTaskCommand`) avant l'action testée, donc `TaskStatusChangedNotifications` contient déjà une entrée à ce stade — assertion corrigée pour chercher la dernière notification correspondant à `IsDone = false` plutôt que `Assert.Single`.
  - **Tests `Api.Tests`** : nouvelle feature `Events/TaskRealtimeNotifications/` (4 scénarios : création, assignation, marquage faite, suppression), vérifiant un vrai message SignalR reçu par un client `HubConnection` réel connecté au hub (`TestServer`/WebSocket in-memory, même mécanisme que `JoinEventHub`), pas seulement que le handler appelle le port — validation de bout en bout du contrat de sérialisation/diffusion. Nécessité de partager la connexion SignalR entre `JoinEventHubSteps` (qui l'ouvre) et les nouveaux steps (qui y attachent des écouteurs `.On<T>`) : connexion déplacée d'un champ privé vers un nouveau contexte Reqnroll `RealtimeHubConnectionContext` (`Support/`, `IAsyncDisposable` — Reqnroll le dispose en fin de scénario comme il le ferait pour une classe `[Binding]`), et logique de construction du `HubConnection` de test extraite dans `HubConnectionTestFactory` (réutilisée par `JoinEventHubSteps`, qui perd son ancien `BuildConnection` privé). **Nommage `RealtimeHubConnectionContext`, pas `HubConnectionContext`** : collision de nom avec le type serveur `Microsoft.AspNetCore.SignalR.HubConnectionContext`, déjà en scope via `using Microsoft.AspNetCore.SignalR;` (erreur `CS0104`). **Piège Reqnroll rencontré** : le step "je me connecte au hub temps réel et que je rejoins cet événement" n'était lié qu'en `[When]` (seul usage jusqu'ici, dans `JoinEventHub.feature`, toujours en position `Quand`) ; utilisé en position `Et`-héritée-de-`Given` dans la nouvelle feature (la connexion y est un préalable, pas l'action testée), Reqnroll ne l'a pas trouvé (steps liés par type de mot-clé résolu, pas seulement par texte) — corrigé en cumulant `[Given]` et `[When]` sur la même méthode plutôt qu'en dupliquant le step. À documenter dans `conventions-code.md` §1.4 si ce piège se reproduit ailleurs.
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement ; `dotnet test` sur les trois projets tous verts — `Domain.Tests` 56 (inchangé), `Application.Tests` 65 (inchangé en nombre, 5 nouveaux `Then` ajoutés aux scénarios existants plutôt que de nouveaux scénarios), `Api.Tests` 71 (67 + 4 nouveaux), Docker/Testcontainers disponible et utilisé, suite `Api.Tests` complète rejouée deux fois de suite sans échec (anomalie de parallélisation connue, cf. lot 4, non reproduite ici). Aucune migration EF nécessaire. Vérification uniquement backend (le câblage frontend du Hub SignalR reste la tâche suivante du lot, plus bas dans ce fichier).
- 2026-09-08 — [Lot 3] Refactoring : pattern Domain Events (mediator maison) pour les handlers de tâches + Unit of Work (`SaveChangesAsync` unique par requête), à la demande explicite du développeur (rule 5, ci-dessus). Revient sur la décision du 2026-09-08 (tâche "Diffusion temps réel des tâches") qui avait écarté les Domain Events faute de second abonné. Détail de l'implémentation et des décisions d'architecture consigné dans `docs/conventions-code.md` §1.2 (Domain Events) plutôt que dupliqué ici. **Décision actée avec le développeur avant implémentation** (question posée explicitement, cf. rule 7) : les handlers de tâches gardent `IEventRepository` en lecture/écriture ; seul `ITaskRealtimeNotifier` en est retiré, déplacé dans des `*DomainEventHandler` dédiés déclenchés après le `SaveChangesAsync` unique — une persistance entièrement pilotée par les domain events (sans appel repository explicite) a été jugée trop risquée pour un projet de cette taille, à contre-courant de la décision du 2026-09-07 (mapping Domain ↔ EF explicite, pas de tracking implicite). **Décisions notables** :
  - `Entity` (Domain) porte désormais la mécanique domain events (`DomainEvents`/`AddDomainEvent`/`ClearDomainEvents`), disponible sur toutes les entités mais utilisée pour l'instant seulement par l'agrégat `Event` (`AddTask`/`AssignTask`/`CompleteTask`/`ReopenTask`/`RemoveTask`). `Reconstitute` ne passe par aucune de ces méthodes : aucun risque de lever un event au chargement depuis la base.
  - **Bug découvert et corrigé pendant la vérification, propre aux fixtures de test** : deux steps Gherkin de `Application.Tests` (`InviteParticipantSteps`/`GetEventByIdSteps`) écrivaient directement via `IUserRepository.AddAsync` hors de tout `ICommandDispatcher.Send` (pas de Command dédiée pour "un compte existe déjà"). Sans le `SaveChangesAsync` implicite qu'avait `AddAsync` jusqu'ici, ces écritures restaient non committées jusqu'au prochain dispatch de commande — un scénario `InviteParticipant` créait alors un second compte pour le même email plutôt que de retrouver le premier. Corrigé en committant explicitement via `IUnitOfWork.SaveChangesAsync` dans ces deux steps (seuls appels directs de ce type trouvés dans `Application.Tests`, vérifié par recherche exhaustive).
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement ; `dotnet test` — `Domain.Tests` 56/56, `Application.Tests` 65/65 (y compris les assertions sur `RecordingTaskRealtimeNotifier`, désormais déclenchées via les domain events plutôt qu'un appel direct du handler — aucun scénario Gherkin existant modifié, ils servent de non-régression), `Api.Tests` : les 31 scénarios liés aux tâches et à `TaskRealtimeNotifications` passent systématiquement en isolation (`--filter`) contre un vrai Postgres/SignalR (Testcontainers) ; sur la suite complète (71 scénarios), 2 échecs intermittents reproduits sur deux runs successifs, à chaque fois sur des scénarios différents et sans rapport (`Update`, `InviteParticipant`) au niveau du step partagé `AuthenticatedSessionSteps` (`RecordingEmailSender.SentEmails`) — confirme l'anomalie de parallélisation déjà connue et suivie séparément (lot 4, "Investiguer la fragilité en exécution parallèle de `EventCo.Api.Tests`"), non liée à ce refactoring. Aucune migration EF nécessaire (aucun changement de schéma). Vérification backend uniquement.
- 2026-09-10 — [Lot 3] Refactoring : repository pattern `ApplyAsync` piloté par les domain events, à la demande explicite du développeur (rule 5, ci-dessus). `IEventRepository`/`IUserRepository`/`IMagicLinkTokenRepository` n'exposent plus qu'une seule méthode d'écriture (`ApplyAsync`, remplace `AddAsync`/`UpdateAsync`) ; `EventMapper.ApplyToEntity`/`SyncChildren` (diff générique de tout l'agrégat `Event`) supprimés, remplacés par des opérations de persistance ciblées (une par domain event, méthode privée dédiée). Détail complet de l'implémentation et des décisions consigné dans `docs/conventions-code.md` §1.2 (Domain Events) plutôt que dupliqué ici. **Décision actée avec le développeur avant implémentation** (question posée explicitement, cf. rule 7) : le pattern s'applique aux trois repositories (`Event`/`User`/`MagicLinkToken`) pour une cohérence complète, malgré l'absence de collection enfant à diffuser pour ces deux derniers. **Décisions notables** :
  - **Domain events ajoutés pour chaque opération de mutation**, y compris celles non exercées par un Command aujourd'hui (`Cancel`/`Complete`/`ConfirmParticipant`/`RemoveParticipant`/`UnassignTask` sur `Event`, `UpdateProfile` sur `User`) : `ApplyAsync` étant devenu le seul chemin d'écriture, une mutation sans event correspondant ne serait silencieusement jamais persistée si un Command venait à l'exercer plus tard. `ApplyAsync` lève une `InvalidOperationException` explicite pour tout domain event non reconnu plutôt que de l'ignorer.
  - `DbContext.FindAsync` utilisé pour résoudre l'entité ciblée par une mise à jour/suppression précise : résout depuis le `ChangeTracker` local sans requête SQL supplémentaire quand l'entité est déjà suivie (typiquement via le `GetByIdAsync` qui précède presque toujours l'appel à `ApplyAsync` dans un même Command), sinon la charge.
  - `IEventRepository.DeleteAsync` (suppression de tout l'agrégat) volontairement laissé **en dehors** du pattern `ApplyAsync` : ce n'est pas une mutation exprimée par un domain event (le Command sait déjà explicitement qu'il veut supprimer), et reste aussi précise qu'elle peut l'être.
- 2026-09-15 — [Lot 3] Endpoint + Query : liste des tâches d'un événement (`GET /api/events/{id}/tasks`), tâche manquante identifiée et ajoutée au backlog avant traitement (rule 5, cf. note ci-dessus). `GetEventTasksQuery`/`GetEventTasksQueryHandler` (`Application/Events/GetEventTasks/`), même forme que `GetEventByIdQuery` (`EnsureCanBeViewedBy` pour l'autorisation, pas de nouvelle logique métier) ; `Event.Tasks` était déjà chargé par `EventRepository.GetByIdAsync` (`.Include(e => e.Tasks)`), donc aucun changement d'infrastructure. Réutilise `EventTaskResponse` (Api), déjà en place pour `POST .../tasks`. **Décisions notables** :
  - Endpoint séparé (`GET /api/events/{id}/tasks`) plutôt qu'ajout de `Tasks` à `EventDetailResponse`/`GetEventByIdQuery` : cohérent avec la route déjà documentée en exemple dans `docs/conventions-code.md` §3.2 (`/api/events/{id}/tasks`), et évite d'alourdir la réponse de consultation d'événement (participants) pour un besoin qui ne concerne que la page de détail côté frontend, où `features/tasks/` interroge son propre endpoint indépendamment de `features/events/`.
  - Tests `Application.Tests` (`Events/GetEventTasks/`, 4 scénarios, InMemory) : classe `[Binding]` autonome avec son propre `ApplicationTestHostBuilder`/base en mémoire, comme les autres features de ce projet (`GetEventByIdSteps`, `CreateTaskSteps`) — le `Given` de création d'événement y est délibérément reformulé (`"... dont je veux consulter les tâches"`) pour éviter une collision de texte de step avec celui, identique par ailleurs, de `GetEventByIdSteps` (`AmbiguousBindingException`, piège déjà documenté en `conventions-code.md` §1.4/§2.3 pour d'autres steps). Vérifiés en conditions réelles : 4/4 verts.
  - Tests `Api.Tests` (`Events/GetEventTasks/`, 4 scénarios) écrits sur le même modèle que `Events/GetById/`, réutilisant le step partagé `Support/ExistingTaskSteps.cs` (`"une tâche ... ajoutée à cet événement via l'API"`) — **non exécutés dans cet environnement** : `docker info` y échoue (`failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`), indisponible pour Testcontainers. `dotnet build` de la solution complète passe sans avertissement (les 4 nouveaux scénarios compilent), mais faute de Docker accessible ici, à rejouer par le développeur avant de considérer la couche `Api.Tests` de cette tâche vérifiée.
- 2026-09-15 — [Lot 3] Frontend : liste des tâches avec filtre par catégorie (`TaskList`, nouvelle feature `client/src/features/tasks/`), affichée dans `EventDetailPage` sous la liste des participants. Filtre par catégorie (`Courses`/`Logistique`/`Autre`/`Toutes`) appliqué côté client sur les tâches déjà chargées via `useEventTasks` (`GET /api/events/{id}/tasks`, tâche backend ajoutée juste au-dessus) — pas de requête réseau par catégorie, cohérent avec un nombre de tâches par événement resté faible dans le cadrage. **Décisions notables** :
  - Sélecteur `<select>` natif labellisé "Filtrer par catégorie" plutôt qu'un groupe de boutons/tabs : plus simple à tester (`getByLabelText`/`selectOptions`) et suffisant sur mobile (pas de bandeau de boutons à faire tenir en largeur), cohérent avec le style déjà minimal des badges de rôle participant sur cette même page.
  - Handler MSW par défaut ajouté pour `GET /api/events/:id/tasks` (`src/test/mocks/handlers.ts`, retourne `[]`) : sans lui, tous les scénarios comportement existants de `EventDetail.feature` auraient cassé (la page appelle désormais systématiquement cet endpoint). Suite comportement complète rejouée : 69/69 verts (dont les 5 nouveaux scénarios `tests/behavior/features/tasks/TaskList.feature`).
  - Non-régression visuelle : `mockEventDetail` (`tests/visual/steps/apparence-detail-evenement.steps.ts`) étendu pour mocker aussi `GET /api/events/event-1/tasks` (2 tâches fixes), sans quoi les 3 scénarios existants de `apparence-detail-evenement.feature` auraient affiché un état d'erreur de chargement des tâches ; nouveau 4ᵉ scénario ajouté pour l'interaction de filtrage. `bddgen` valide les 4 scénarios (aucune étape manquante) et un run manuel du nouveau scénario confirme le rendu (page + mocks + filtre fonctionnels, seule capture de référence absente, comme attendu pour tout nouveau scénario). Capture générée localement supprimée après vérification (jamais une baseline valide sous Windows, cf. `docs/conventions-code.md` §2.3).
- 2026-09-15 — [Lot 3] Frontend : connexion au Hub SignalR et mise à jour réactive de la liste de tâches. Nouveau hook `useTaskRealtime` (`client/src/features/tasks/hooks/`), branché dans `TaskList` : ouvre une connexion `@microsoft/signalr` vers `/hubs/events` (`shared/lib/signalr.ts`, `withCredentials: true` pour le cookie de session), rejoint le groupe de l'événement (`connection.invoke('JoinEvent', eventId)`) et met à jour directement le cache React Query de la clé `['events', eventId, 'tasks']` (`queryClient.setQueryData`, pas d'`invalidateQueries` — évite un aller-retour réseau superflu à chaque message) sur réception de `TaskCreated`/`TaskAssigned`/`TaskStatusChanged` (upsert) et `TaskDeleted` (retrait). **Décisions notables** :
  - `@microsoft/signalr` ajouté aux dépendances (`client/package.json`) : lib officielle Microsoft, seul client compatible avec le protocole du hub déjà en place côté backend (`EventCo.Infrastructure/Realtime/EventHub.cs`) — pas d'alternative pertinente à évaluer ici, contrairement aux cas MediatR/SpecFlow déjà rencontrés.
  - Payloads du hub mappés vers `EventTask` dans le hook plutôt que de faire porter cette forme par le type partagé : `TaskRealtimeDto`/`TaskDeletedRealtimeDto` (backend) exposent `TaskId` (→ `taskId` en JSON, camelCase par défaut du `JsonHubProtocol`) là où `EventTask` (frontend, calqué sur `GET /api/events/{id}/tasks`) expose `id` — nouveaux types `TaskRealtimePayload`/`TaskDeletedRealtimePayload` (`features/tasks/types.ts`) pour documenter cette forme distincte plutôt que de forcer une correspondance artificielle.
  - **Point d'architecture non couvert par `docs/conventions-code.md`, signalé pour validation (rule 7)** : la connexion Hub est ouverte/fermée dans un hook dédié à la durée de vie de `TaskList` (un abonnement par montage de la page de détail), plutôt que centralisée dans un contexte applicatif partagé entre pages — cohérent avec le besoin actuel (un seul écran consomme les événements de tâches) mais à revisiter si un futur lot (ex. notifications globales, lot 5) doit réagir aux mêmes messages ailleurs dans l'app.
  - **Stratégie de test, également signalée pour validation (rule 7)** : le websocket du Hub n'est pas interceptable par MSW comme le sont les appels `fetch` des autres tests de comportement (`server.listen({ onUnhandledRequest: 'error' })` ne couvre que HTTP) ; nouvelle feature `tests/behavior/features/tasks/TaskListRealtime.feature` (3 scénarios) construite en mockant directement le module `@microsoft/signalr` (`vi.mock`, seul usage de ce mécanisme dans le projet à ce jour — tout le reste passe par MSW) pour simuler les messages du hub et vérifier la réaction de `TaskList`. Écart assumé faute de précédent établi dans `conventions-code.md` pour ce cas (temps réel), à documenter en §2.3 si cette approche est validée.
  - Vérifié : suite de tests frontend complète rejouée, 78/78 verts (dont les 3 nouveaux scénarios temps réel) ; `tsc -b` sans erreur. Pas de vérification manuelle contre un vrai back (Docker indisponible dans cet environnement, cf. note du 2026-09-15 ci-dessus sur `Api.Tests`) — à confirmer par le développeur en conditions réelles (deux onglets sur le même événement, actions sur l'un reflétées sur l'autre sans rechargement).
  - Vérifié en conditions réelles : `dotnet build` de la solution complète sans avertissement (la disparition d'`AddAsync`/`UpdateAsync` des interfaces a fait remonter tous les appelants à corriger en erreur de compilation, aucun oubli) ; `dotnet test` — `Domain.Tests` 56/56, `Application.Tests` 65/65, `Api.Tests` 71/71 (Docker/Testcontainers disponible, exécuté plusieurs fois — un seul run a reproduit l'anomalie de parallélisation déjà connue et suivie séparément, lot 4, à chaque fois sur un scénario différent et sans rapport avec ce refactoring ; un run complet et un run filtré sur les scénarios exerçant les nouvelles opérations précises — `PromoteToOrganizer`/`DemoteToParticipant`/`InviteParticipant`/`Update`/`CreateEvent`/`Verify`/`RequestLink`/`Delete` — confirment le bon fonctionnement contre un vrai Postgres). Aucune migration EF nécessaire (aucun changement de schéma). Vérification backend uniquement.
- 2026-09-17 — [Lot 3] Frontend : formulaire d'ajout de tâche (titre, catégorie, quantité). Nouveau `AddTaskForm` (`client/src/features/tasks/components/`), affiché dans `EventDetailPage` sous `TaskList` pour tout participant de l'événement (créateur ou participant listé, y compris invitation en attente — la règle `Event.AddTask` côté Domain n'exige que l'appartenance à `Participants`, pas `hasJoined`) plutôt que réservé aux organisateurs, cohérent avec `EnsureActingUserIsParticipant` (pas de restriction par rôle) côté `Event.AddTask`. **Décisions notables** :
  - `useCreateTask` met à jour directement le cache React Query (`queryClient.setQueryData` sur la clé `['events', eventId, 'tasks']`) plutôt que d'invalider la requête : cohérent avec `useTaskRealtime` (même clé, même stratégie d'upsert par `id` déjà en place), et la diffusion SignalR `TaskCreated` (déjà câblée) mettrait de toute façon à jour le même cache juste après — l'upsert par `id` rend les deux mises à jour idempotentes l'une envers l'autre, sans doublon possible.
  - Comportement (`tests/behavior/features/tasks/AddTask.feature`, 3 scénarios) : suite complète frontend rejouée, 89/89 verts. Non-régression visuelle : nouveau scénario "formulaire d'ajout de tâche rempli" ajouté à `apparence-detail-evenement.feature`/`.steps.ts` — **les 4 captures de référence déjà existantes sur cette feature sont désormais périmées** (le formulaire apparaît maintenant sur toutes les vues du détail d'événement) et devront être régénérées avec la nouvelle, via le workflow `update-visual-baselines` (jamais localement, cf. `conventions-code.md` §2.3) avant la prochaine review de lot.
- 2026-09-17 — [Lot 3] Frontend : interaction rapide pour cocher une tâche (optimisée mobile) — dernière tâche du lot. La case "Faite/À faire" (texte) de `TaskList` est remplacée par une vraie case à cocher (`<input type="checkbox">`, zone tactile 44×44 via un `<label>` englobant, cf. cadrage §"Ergonomie tactile" — zones cliquables larges), reliée à un nouveau hook `useToggleTaskDone` (`client/src/features/tasks/hooks/`) qui appelle `POST .../tasks/{taskId}/complete` ou `.../reopen` (`api.ts`, endpoints déjà exposés depuis ce lot) selon `task.isDone`. **Décisions notables** :
  - **Mise à jour optimiste du cache React Query** (`onMutate` bascule `isDone` immédiatement dans `queryClient.setQueryData`, `onError` restaure l'état précédent) : première utilisation de ce pattern dans le projet (les mutations existantes — `useCreateTask`, `usePromoteParticipant`... — attendent la réponse avant de mettre à jour le cache). Choix directement motivé par le libellé de la tâche ("interaction *rapide*") : la confirmation serveur/SignalR (`useTaskRealtime`, déjà en place) arrive de toute façon peu après et réconcilie l'état sans action supplémentaire, l'upsert par `id` du hook temps réel restant idempotent avec cette mise à jour locale.
  - **Autorisation frontend alignée sur la règle Domain `Event.EnsureActingUserCanToggleTaskDone`** (créateur/co-organisateur : toute tâche ; simple participant : uniquement une tâche qui lui est assignée) : `EventDetailPage` calcule désormais `isCreatorOrOrganizer` (variable extraite, réutilisée par `canInvite` qui portait déjà exactement ce calcul) et le transmet à `TaskList` avec l'id de l'utilisateur courant, pour désactiver la case des tâches qu'il ne peut pas cocher plutôt que de laisser l'API renvoyer un 403 après coup. Reste une réplique côté client d'une règle Domain (pas de nouvelle source de vérité) : l'API continue de rejeter le cas échéant.
  - Comportement (`tests/behavior/features/tasks/ToggleTask.feature`, nouvelle feature, 5 scénarios : coche/décoche par le créateur, coche par un participant sur sa propre tâche, case désactivée pour une tâche non assignée à un simple participant, rollback optimiste sur échec de l'API) + mise à jour de l'assertion existante `TaskListRealtime.feature` (`toBeChecked()` sur la case plutôt que le texte "Faite", disparu). Suite complète frontend rejouée : 106/106 verts. `tsc --noEmit` et `oxlint` propres (un seul warning préexistant, sans rapport, sur `TaskListRealtime.steps.tsx`).
  - Non-régression visuelle **non mise à jour dans cette tâche** : la case à cocher remplace le badge de statut sur `apparence-detail-evenement.feature`, périmant à nouveau les captures de référence (déjà signalées périmées par la tâche précédente, ci-dessus) ; pas de nouveau scénario nécessaire (l'apparence des scénarios existants suffit à couvrir le changement), régénération à faire avec celle déjà en attente via `update-visual-baselines`, non bloquant (cf. `docs/conventions-code.md` §2.3, capture non générée localement).
  - **Toutes les tâches Must have du Lot 3 sont désormais cochées — review du lot à déclencher avant de démarrer le Lot 4** (rule 6).- 2026-09-17 — [Lot 4] Attribut `data-testid` sur tout composant/élément testable du frontend (formulaires, champs, boutons, liens, messages, badges, lignes de liste), tâche identifiée en cours de route et ajoutée au backlog avant traitement (rule 5), à la demande explicite du développeur. Les trois couches de tests (comportement Vitest, non-régression visuelle et E2E Playwright) sélectionnent désormais exclusivement via ce test-id plutôt que par rôle/label/texte affiché. Décisions et convention de nommage détaillées dans `docs/conventions-code.md` §2.3 (dont le suffixage par id métier pour les lignes de liste répétées, et son cas particulier en E2E où l'id est retenu depuis la réponse de l'API de création plutôt que connu à l'avance). Suite comportement rejouée : 106/106 verts ; `tsc --noEmit` et `bddgen`/`playwright test --list` (33 scénarios visuels) propres ; run visuel local sans échec de sélection (seuls des écarts de pixels Windows/Linux déjà documentés). E2E non rejouée dans cet environnement (Docker indisponible), vérifiée par compilation et génération des specs uniquement.
